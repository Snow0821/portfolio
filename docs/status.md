# Project Status

## 진행 중

- 페이지 패키지와 컴포넌트 중심 구조 개편 실행 준비

## 확인된 문제

- 슬라이드 헤더가 연결 후 설정된 속성을 다시 렌더링하지 않는다.
- 가로 PDF 임시 DOM에 `.slide-container.horizontal` 구조가 없다.
- PDF 생성 실패 시 임시 DOM이 남을 수 있다.
- PowerShell 구조 검증 스크립트가 삭제된 CSS 경로를 요구하며 현재 환경에는 PowerShell 실행기도 없다.
- 세미나 안내의 Markdown 강조 문법이 HTML에서 그대로 노출된다.
- PDF 다운로드 버튼의 접근성 이름이 주제와 문서 형식을 구분하지 못한다.
- 페이지, 컴포넌트, 콘텐츠와 CSS의 책임 경계가 디렉터리에 일관되게 반영되지 않았다.

## 다음 작업

1. `docs/plan.md` Task 1의 문서 가드레일을 적용한다.
2. 공통 기반과 홈 콘텐츠를 새 구조로 이전한다.
3. 세미나와 프레젠테이션 페이지 패키지를 구성한다.
4. PDF 서비스를 분리하고 확인된 동작 문제를 수정한다.
5. 레거시 구조를 제거하고 자동·브라우저 검증을 수행한다.

## 보류

- 외부에 공개된 기존 URL이 확인되면 호환 리다이렉트 정책을 다시 검토한다.

## 최근 완료

- 2026-08-03: 프로젝트 코드 리뷰 및 개선점 기록
- 2026-08-03: 페이지 패키지·공용 컴포넌트·공통 CSS·문서 이력 체계 설계
