# 2026 Work History

## 2026-08-03 Seminar content contract and feature-local test runner

### Initial task list and success criteria

1. Add feature-local seminar test discovery and a contract fixture for the
   `introductory-60` topic shape.
2. Define a validator that returns valid source objects and aggregates
   field-aware contract errors.
3. Document the feature boundary and author-facing format rules without
   changing public page behavior or URLs.
4. Verify the focused contract, complete test suite, and whitespace-only diff.

### Discovery, resolution, and evidence

- `resolved`: The feature-local test import initially failed because
  `features/seminars/data/validation.js` did not exist. RED evidence:
  `node --test features/seminars/tests/contract.test.mjs` exited 1 with
  `ERR_MODULE_NOT_FOUND` for that exact path.
- `resolved`: Added the `introductory-60` validator, local fixtures, and
  feature-local test discovery. GREEN evidence:
  `node --test features/seminars/tests/contract.test.mjs` passed 6 of 6 tests.
- `resolved`: Documented the public-boundary plan, validation lifecycle,
  authoring rules, shared-source responsibilities, accessibility image rules,
  and evidence-based format evolution in the feature-local documents.
- `resolved`: `npm.cmd test` passed 23 of 23 tests: the six feature contract
  tests plus the existing 17 repository tests. `git diff --check` exited 0;
  Git reported only LF-to-CRLF working-copy warnings.

### Remaining work

- Task 2 will migrate production topics to this contract; Task 1 adds no page
  imports, data migration, or user-visible behavior changes.

## 2026-08-03 Introductory seminar shared-source migration

### Goal, task list, and success criteria

1. Move the Python and Web introductory seminar facts into feature-local,
   structured sources without changing pages, public URLs, or the legacy
   registry used during coexistence.
2. Expose a lazy-validated feature registry for the two topics and preserve
   titles, author, summaries, tags, and the existing Python and HTML/CSS code
   examples.
3. Add data tests for registry behavior, the five semantic roles, presentation
   role coverage, legacy-view key removal, and topic-local production images.
4. Record focused and full-suite verification before committing this task.

Success requires both sources to validate only when requested, contain the
five roles in order, use the required block IDs and nine-slide presentation
pattern, and leave all existing page behavior untouched.

### Discovery, resolution, and evidence

- `resolved`: The RED data test was added before feature-local production data.
  `node --test features/seminars/tests/data.test.mjs` exited 1 with
  `ERR_MODULE_NOT_FOUND` for
  `features/seminars/data/topics/python-intro.js`; the missing source was the
  intended failure.
- `resolved`: Added the Python and Web shared sources with source blocks for
  problem, prior art, method, cases, and conclusion. Each topic has nine slides
  in the required cover, agenda, six-content, outro pattern; the Python and
  Web code slides use `split` layout.
- `resolved`: The feature registry freezes its source collections but calls
  `validateSeminar` only from `getSeminarList()` and `getSeminar(id)`, avoiding
  import-time validation failures that later page orchestration must render.
- `confirmed`: Neither existing introductory source included an image block.
  The new production-image test walks every migrated block and will verify any
  future origin-rooted image path without adding an invented asset.
- `GREEN`: `node --test features/seminars/tests/data.test.mjs` passed 3 of 3
  tests. `node --test features/seminars/tests/contract.test.mjs
  features/seminars/tests/data.test.mjs` passed 10 of 10 tests.
- `GREEN`: `npm.cmd test` passed 27 of 27 tests. `git diff --check` exited 0
  with no whitespace errors.

### Remaining work

- Task 3 will render these structured sources. Pages deliberately continue to
  use the legacy registry until the later orchestration task; no browser check
  is required for this data-only coexistence step.

## 2026-08-03 Introductory seminar content-preservation review fix

### Finding, RED evidence, and resolution

- `resolved`: Review found that the migrated summaries had been paraphrased
  and that each topic retained only one of its two legacy code examples. This
  corrected the earlier preservation claim: both original examples now remain
  in the shared method section, while only the prescribed code slide references
  the presentation-oriented example.
- `RED`: The strengthened data test asserted the exact legacy summaries,
  visible section titles, required block IDs, nine-slide type/role/layout plan,
  and both complete source strings. `node --test
  features/seminars/tests/data.test.mjs` failed 1 of 4 tests because the Python
  summary differed from the legacy source.
- `resolved`: Restored both summaries byte-for-byte, restored each selected
  presentation code block byte-for-byte including comments, and added
  `python-method-reading-code` and `web-method-reading-code` as unreferenced
  shared reading blocks. The nine-slide plans and required block IDs remain
  unchanged.
- `GREEN`: `node --test features/seminars/tests/data.test.mjs` passed 4 of 4
  tests. `node --test features/seminars/tests/contract.test.mjs
  features/seminars/tests/data.test.mjs` passed 11 of 11 tests.
- `GREEN`: `npm.cmd test` passed 28 of 28 tests. `git diff --check` exited 0
  with no whitespace errors. Executable and test files remain at or below 200
  lines: registry 17 and data test 162; topic data files are 152 and 151 lines.

### Remaining work

- Task 3 will consume the additional reading-oriented source blocks without
  adding slides or altering existing public pages and URLs.

## 2026-08-03 Python seminar f-string preservation re-review fix

### Finding, RED evidence, and resolution

- `resolved`: A second review found that both restored Python examples still
  contained a literal `$` before the f-string braces. The previous
  byte-for-byte preservation statement was therefore premature for those two
  lines, and the test repeated the same incorrect expectation.
- `RED`: Updated the runtime data-test expectations to the exact legacy
  `print(f"Hello, {name}!")` and `print(f"Hello, {user_name}!")` strings.
  `node --test features/seminars/tests/data.test.mjs` failed 1 of 4 tests with
  actual `print(f"Hello, ${name}!")`, directly proving the defect.
- `resolved`: Removed the two unintended dollar signs from the source template
  literals and retained ordinary non-interpolating test template text for the
  legacy Python f-strings.
- `GREEN`: `node --test features/seminars/tests/data.test.mjs` passed 4 of 4
  tests. `node --test features/seminars/tests/contract.test.mjs
  features/seminars/tests/data.test.mjs` passed 11 of 11 tests.
- `GREEN`: `npm.cmd test` passed 28 of 28 tests. `git diff --check` exited 0
  with no whitespace errors. Executable and test files remain at or below 200
  lines: registry 17, data test 162, Python topic 152, Web topic 151.

### Remaining work

- Task 3 remains responsible for rendering the preserved shared source blocks.

## 2026-08-03 Seminar slide-reference contract review fix

### Finding, RED evidence, and resolution

- `resolved`: Review found that a content slide could only reference blocks
  owned by its `sectionRole`, although the contract requires at least one
  owned reference and permits additional known references from other sections.
- RED: Added a real validator test with a `problem` slide that references its
  own `sample-problem` block and the known `sample-prior-art` block. The focused
  command `node --test features/seminars/tests/contract.test.mjs` failed 1/7
  with `presentation.slides[2].blockIds[1] must belong to problem`.
- `resolved`: The validator now still rejects missing references and empty
  `blockIds`, accepts known cross-section references, and reports an error only
  when no known reference belongs to the slide's `sectionRole`.
- GREEN: `node --test features/seminars/tests/contract.test.mjs` passed 7/7.
  `npm.cmd test` passed 24/24 (the seven contract and 17 existing tests), and
  `git diff --check` exited 0 with only LF-to-CRLF working-copy warnings.

## 2026-08-03 — 페이지·컴포넌트 중심 구조 설계

### 요청과 목적

- `index.html`을 제외한 화면을 명시적인 페이지 패키지로 구성한다.
- 페이지 모듈은 조립에 집중하고 실제 구현은 컴포넌트 중심으로 구성한다.
- CSS는 가능한 한 공통 계층을 공유한다.
- 모든 변경 작업에 문서화와 작업 이력이 동반되도록 강제한다.
- 현재 구조와 과거 변경 과정을 각각 쉽게 찾을 수 있는 문서 체계를 만든다.

### 파악한 문제

- `level1-*`부터 `level4-*`까지의 디렉터리명이 책임보다 추상적인 계층을 표현한다.
- 페이지 초기화 코드가 HTML 안에 포함되어 있고 공용 컴포넌트와 페이지 전용 코드가 혼재한다.
- 세미나 콘텐츠와 리다이렉트 페이지가 중복되어 단일 출처가 불명확하다.
- 기존 구조 검증 스크립트가 삭제된 CSS 경로를 계속 요구한다.
- 프로젝트 구조, 구현 철학, 현재 문제와 과거 결정의 공식 기록 위치가 없다.

### 결정과 근거

- 빌드 도구 없이 동작하는 페이지 패키지 구조를 선택했다.
- 공용 UI는 `components/`, UI가 아닌 기능은 `services/`, 정적 콘텐츠는 `content/`로 분리한다.
- CSS는 `tokens → base → layouts → components → page.css` 순서로 의존한다.
- 현재 사실은 `architecture.md`, `conventions.md`, `decisions.md`, `status.md`에서 유지하고 변경 과정은 연도별 이력에 기록한다.
- 작업별 로그와 개별 ADR 파일은 현재 규모에 비해 관리 비용이 크므로 만들지 않는다.
- 프로젝트 파일을 변경한 작업은 관련 현재 문서와 이력을 함께 갱신해야 완료된 것으로 간주한다.
- 검증 스크립트는 현재 환경에 없는 Node나 PowerShell 대신 사용 가능한 Python 3 표준 라이브러리로 작성한다.

### 수행한 작업

- 승인된 목표 구조와 원칙을 `docs/architecture.md`에 문서화했다.
- 구조 개편의 검증 기준과 문서 유지 전략을 명시했다.
- 설계 단계의 작업 이력을 이 문서에 기록했다.
- 구현 전 현재 상태와 다음 단계를 `docs/status.md`에 기록했다.
- 사용자 설계 승인 후 파일별 책임, 테스트 우선 순서, 커밋 경계를 포함한 실행 계획을 `docs/plan.md`에 작성했다.

### 검증 결과

- 설계 문서에서 `TBD`, `TODO`, 미정 요구사항을 검색했다.
- 목표 구조, 의존 방향, URL 정책, 초기 개편 범위와 검증 기준 사이의 모순이 없는지 확인했다.
- Markdown 공백 오류를 `git diff --check`로 확인했다.
- `command -v node`, `command -v pwsh`, `command -v python3`과 각 버전 확인으로 검증기 실행 환경을 조사했다. Node와 PowerShell은 없고 Python 3.14.3을 사용할 수 있음을 확인했다.
- 구현 계획에서 승인된 설계 요구사항이 각 작업과 최종 성공 기준에 연결되는지 검토하고, 미래 작업에 대한 선행 의존성이 없도록 작업 인터페이스를 정정했다.

### 남은 작업

- `docs/plan.md`의 작업 순서에 따라 구조 개편과 동작 문제 수정을 구현한다.
- 각 작업에서 자동 검증, 필요한 브라우저 검증과 문서 갱신을 함께 수행한다.

## 2026-08-03 — Vercel 로컬 개발 환경 설계 보완

### 요청과 목적

- Vercel에 코드를 올리기 전에 정적 페이지와 향후 API를 로컬에서 함께 확인한다.
- 로컬 시스템에 Node.js를 직접 설치하지 않고 개발·검증 버전을 통일한다.
- 향후 API 기능을 같은 저장소 안에서 자연스럽게 확장할 수 있게 한다.

### 파악한 문제

- 현재 로컬 환경에는 Node와 PowerShell이 없어 기존 계획의 JavaScript 또는 PowerShell 검증기를 직접 실행할 수 없다.
- Python 정적 서버는 HTML·CSS·클라이언트 JavaScript를 제공할 수 있지만 Vercel Functions 실행 환경을 재현하지 못한다.
- Vercel 배포용 컨테이너를 도입하면 정적 사이트와 작은 Functions에 비해 운영 모델이 불필요하게 복잡해진다.

### 결정과 근거

- Docker는 운영 배포물이 아니라 로컬 개발과 검증 환경으로만 사용한다.
- Docker 안에 Vercel 기본 런타임과 맞춘 Node.js 24 및 Vercel CLI를 설치한다.
- 이 결정은 같은 날 앞서 계획한 로컬 Python 구조 검증기를 Docker 내부 Node.js 검증기로 대체한다.
- 로컬 통합 실행은 Vercel 공식 개발 명령인 `vercel dev`를 사용한다.
- 프런트엔드는 빌드 없는 정적 구조를 유지하고 API만 `api/*.mjs` Vercel Functions로 확장한다.
- 실제 프런트엔드 번들링 요구가 생기기 전에는 Vite를 도입하지 않는다.
- 운영과 Preview 배포는 기존 Git→Vercel 흐름을 유지하고 `Dockerfile.vercel`은 만들지 않는다.

### 수행한 작업

- `docker --version`과 `docker compose version`으로 로컬 Docker 사용 가능 여부를 확인했다.
- Docker 기반 개발 환경, `/api/health`, Node 구조 검증기를 목표 구조와 검증 기준에 추가했다.

### 검증 결과

- Docker 29.5.3과 Docker Compose 5.1.4를 사용할 수 있음을 확인했다.
- 저장소에 기존 `.vercel`, `vercel.json`, `package.json`, lockfile이 없음을 확인했다.

### 남은 작업

- 수정된 설계를 반영해 `docs/plan.md`의 Python 검증 단계를 Docker/Node 검증 단계로 교체한다.
- Docker 개발 환경, Node 도구 체인과 health function을 구현한다.
- Vercel 계정 연결이 필요한 로컬 Functions 및 Preview 검증은 사용자 승인 범위 안에서 수행한다.

## 2026-08-03 — fnm 기반 개발 환경으로 경량화

### 요청과 목적

- Docker 컨테이너 안에서 상시 개발할 때 생기는 실행과 디버깅 마찰을 피한다.
- Homebrew를 사용하지 않고 프로젝트별 Node.js 버전을 관리한다.
- 새 작업자가 동일한 개발 환경을 준비할 수 있도록 기본 설치와 사용법을 README에 기록한다.

### 파악한 문제

- Docker는 현재 정적 프런트엔드와 소규모 Vercel Functions에 비해 개발 환경이 무겁다.
- Homebrew는 사용자 환경에서 기존 도구와 충돌한 경험이 있어 설치 경로로 사용할 수 없다.
- `fnm` 바이너리는 `~/.local/bin/fnm`에 설치되었지만 셸 `PATH` 설정은 사용자가 명시적으로 관리해야 한다.

### 결정과 근거

- 같은 날 작성한 개발 전용 Docker 설계를 철회하고 `fnm` 기반 Node.js 24 환경으로 대체한다.
- `fnm`은 Homebrew 없이 `~/.local/bin`에 설치하고 zsh 초기화 두 줄만 사용자가 관리한다.
- Node.js 버전은 `.node-version`, Vercel CLI와 JavaScript 의존성은 프로젝트 `package.json`과 lockfile로 고정한다.
- Python 정적 서버는 Node/Vercel 도구가 구성되기 전까지의 단순 페이지 확인 방법으로 유지한다.

### 수행한 작업

- `README.md`에 Homebrew 없는 `fnm` 설치, zsh 연결, Node.js 24 설치와 확인 방법을 기록했다.
- 현재 실행 가능한 Python 정적 서버와 향후 `npm run dev`, `npm test` 흐름을 구분해 기록했다.
- `docs/architecture.md`와 `docs/status.md`에서 Docker 개발 환경을 `fnm` 기반 환경으로 정정했다.
- `docs/plan.md`의 Python 검증 계획을 Node.js 구조 검증과 Vercel Functions 기반으로 갱신했다.

### 검증 결과

- `~/.local/bin/fnm`에서 fnm 1.39.0이 실행되는 것을 확인했다.
- fnm에 Node.js v24.18.1이 기본 버전으로 설치된 것을 확인했다.
- 프로젝트 루트의 사용자 생성 `.node-version` 값이 `24`인 것을 확인했다.
- README 명령이 현재 파일 상태와 앞으로 추가될 `package.json` 상태를 혼동하지 않도록 구분했으며 Markdown 공백 검사를 수행했다.

### 남은 작업

- `package.json`, lockfile, 프로젝트 로컬 Vercel CLI, Node 구조 검증기와 `/api/health`를 구현한다.
- 사용자 셸에서 `fnm --version`, `node --version`, `npm --version`을 확인한다.

## 2026-08-03 — Node/Vercel 개발 기반과 문서 가드레일 적용

### 요청과 목적

- 승인된 구조 개편 계획의 첫 단계로 로컬 실행, API 확장과 자동 검증 기반을 만든다.
- 모든 프로젝트 변경에 현재 문서와 연도별 작업 이력을 동반하는 규칙을 저장소 수준에서 명시한다.

### 파악한 문제

- 프로젝트에 `package.json`, lockfile과 자동 검증기가 없어 Node 및 Vercel CLI 버전과 검증 명령이 고정되지 않았다.
- Vercel Functions의 로컬·Preview 동작을 확인할 최소 API가 없었다.
- 문서화 원칙이 설계 문서에만 있고 이후 작업자가 즉시 발견할 루트 작업 규칙이 없었다.
- Vercel CLI 58.4.4 설치 결과 전이 의존성에서 30개 감사 항목과 `tar`, `stream-to-promise` 사용 중단 경고가 보고됐다.

### 결정과 근거

- `.node-version`과 `package.json#engines.node`를 Node.js 24로 맞추고 Vercel CLI 58.4.4를 개발 의존성으로 고정했다.
- 자동 검증은 추가 프레임워크 없이 Node 표준 테스트 러너를 사용한다.
- 루트 `AGENTS.md`에 문서화, 구조, 개발 환경과 완료 전 검증 규칙을 두고 상세 규칙은 `docs/conventions.md`에서 관리한다.
- 전이 의존성 경고는 강제 업그레이드로 lockfile을 불안정하게 바꾸지 않고 현재 리스크로 기록해 상위 Vercel CLI 갱신 시 재검토한다.

### 수행한 작업

- `.gitignore`, `package.json`, `package-lock.json`과 `tests/verify-structure.mjs`를 추가했다.
- 프로젝트 로컬 `vercel dev`와 Node 표준 테스트 명령을 npm 스크립트로 제공했다.
- HTTP 200과 `{ "status": "ok" }`를 반환하는 `api/health.mjs`를 추가했다.
- `AGENTS.md`, `docs/conventions.md`, `docs/decisions.md`를 추가하고 README의 설치 및 실행 흐름을 현재 상태로 갱신했다.

### 검증 결과

- 구현 전에 구조 검증 테스트를 실행해 개발·문서 파일과 health function이 없어서 실패하는 것을 확인했다.
- `npm install`로 278개 패키지를 설치하고 lockfile을 생성했다.
- `npm test`에서 필수 파일, Node.js 24 메타데이터와 health function 응답을 검증하는 테스트 3개가 모두 통과했다.
- `git diff --check`가 공백 오류 없이 통과했다.
- zsh 로그인 환경에서 fnm 1.39.0, Node.js v24.18.1과 npm 11.16.0을 확인했다.

### 남은 작업

- 공통 CSS, 사이트 헤더와 홈 콘텐츠를 새 책임 기반 구조로 이전한다.
- Vercel Functions 라우팅은 이후 `npm run dev` 통합 검증에서 확인한다.
- Vercel CLI 상위 버전이 제공될 때 기록된 전이 의존성 감사 항목을 다시 확인한다.

## 2026-08-03 — 공통 사이트 기반과 홈 콘텐츠 이전

### 요청과 목적

- 홈 화면을 페이지 조립과 공용 컴포넌트, 정적 콘텐츠 책임으로 분리한다.
- 계층 번호에 의존하던 CSS를 모든 페이지가 공유할 수 있는 의미 기반 진입점으로 바꾼다.

### 파악한 문제

- 홈 헤더가 `index.html`에 직접 작성되어 세미나 페이지와 일관된 내비게이션을 공유하기 어렵다.
- 홈 콘텐츠가 일반 `sections/`에 있고 CSS가 `level1`, `level2` 경로에 나뉘어 실제 책임을 경로에서 알기 어렵다.
- 기존 헤더 CSS의 `body > header` 선택자는 사용자 정의 요소 안에 렌더링되는 공용 헤더에 적용할 수 없다.

### 결정과 근거

- 사이트 헤더는 `home-href`, `seminars-href`, `current` 속성만 입력받는 반응형 Web Component로 만들었다.
- 헤더 마크업 생성 함수를 분리해 현재 메뉴 표시를 DOM 환경 없이도 자동 검증할 수 있게 했다.
- 공통 CSS 진입점은 `tokens → base → layouts → components → print` 순서를 명시하고 기존 파일은 최종 레거시 정리 단계까지 보존한다.
- 홈 정적 조각은 `content/home/`로 옮기고 기존 비동기 `section-include` 동작과 콘텐츠를 그대로 유지했다.

### 수행한 작업

- `components/site-header.js`와 공용 헤더 스타일을 추가하고 홈 마크업을 `<site-header>`로 교체했다.
- `styles/main.css`, `styles/base.css`, `styles/layouts.css`, `styles/components/content-section.css`를 구성했다.
- 소개, 연구, 강의 경력과 교육 콘텐츠 네 개를 `content/home/`에 추가하고 홈 참조를 새 경로로 갱신했다.
- 인쇄 시 새 사용자 정의 헤더가 숨겨지도록 공통 인쇄 규칙을 수정했다.

### 검증 결과

- 구현 전에 공통 기반 필수 파일과 홈 참조, 현재 메뉴 동작 테스트가 누락 파일 때문에 실패하는 것을 확인했다.
- `npm test`에서 기존 기반 테스트와 새 홈 구조·헤더 동작 테스트 5개가 모두 통과했다.
- Python 정적 서버와 실제 Chrome 브라우저에서 홈의 소개를 포함한 다섯 섹션, `About` 현재 메뉴와 새 세미나 경로가 렌더링되는 것을 확인했다.
- 브라우저 콘솔에 오류나 경고가 없고 가로 오버플로가 없음을 확인했다.
- 390×844 모바일 뷰포트에서 사이트 헤더, 내비게이션과 홈 인트로가 가로 오버플로 없이 배치되는 것을 확인했다.
- `git diff --check`가 공백 오류 없이 통과했다.

### 남은 작업

- 세미나 목록을 `pages/seminars/` 페이지 패키지와 접근 가능한 목록 컴포넌트로 이전한다.
- 최종 정리 단계에서 홈의 기존 `sections/` 사본과 계층 기반 CSS를 제거한다.

## 2026-08-03 — 세미나 페이지 패키지와 목록 컴포넌트 이전

### 요청과 목적

- 루트에 있던 세미나 진입 화면을 명시적인 페이지 패키지로 옮긴다.
- 카드 렌더링이 데이터, URL과 PDF 구현을 직접 소유하지 않는 재사용 컴포넌트가 되게 한다.
- 아이콘 다운로드 버튼마다 주제와 형식을 구분할 수 있는 접근성 이름을 제공한다.

### 파악한 문제

- 기존 세미나 renderer가 세미나 데이터, 프레젠테이션 URL, PDF DOM 생성과 다운로드를 한 파일에서 모두 담당했다.
- 프레젠테이션 URL이 기존 `slides/` 경로로 하드코딩되어 있었다.
- 다운로드 버튼의 보이는 내용이 같은 📥 아이콘뿐이라 보조 기술에서 주제와 문서 형식을 구분하기 어려웠다.
- 소개 문장에 Markdown 강조 기호가 HTML 텍스트로 그대로 노출됐다.

### 결정과 근거

- 목록 컴포넌트는 `seminars`, `paths`, `onDownload`를 입력받아 데이터 출처, URL 정책과 PDF 구현에 의존하지 않게 했다.
- 읽기용과 발표용 링크 생성은 페이지 모듈이 담당하고 PDF 서비스가 추가되기 전에는 인쇄 기능 안내를 명시적으로 제공한다.
- 카드와 액션의 재사용 표현은 공통 컴포넌트 CSS에, 세미나 소개 헤더 배치만 페이지 CSS에 두었다.

### 수행한 작업

- `pages/seminars/`에 HTML, `page.js`, `page.css`로 구성된 페이지 패키지를 추가했다.
- `components/seminar-list.js`에 입력 기반 카드 렌더링과 다운로드 callback 연결을 구현했다.
- 네 다운로드 버튼에 주제명과 읽기·발표 형식을 포함한 `aria-label`을 생성했다.
- `styles/components/seminar-card.css`를 만들고 공통 `styles/main.css`에서 불러오도록 했다.
- 세미나 소개의 Markdown 별표를 시맨틱 `<strong>` 요소로 교체했다.

### 검증 결과

- 구현 전에 페이지 패키지와 목록 컴포넌트가 없어 새 구조·접근성 계약 테스트가 실패하는 것을 확인했다.
- `npm test`에서 새 presentation URL, 다운로드 접근성 이름과 callback 전달을 포함한 테스트 6개가 모두 통과했다.
- 실제 Chrome 브라우저에서 세미나 카드 2개, 새 presentation 링크 4개와 서로 다른 다운로드 버튼 이름 4개를 확인했다.
- `Seminars` 현재 메뉴 표시, Markdown 별표 부재와 브라우저 콘솔 오류·경고 부재를 확인했다.
- 390×844 모바일 뷰포트에서 카드와 액션 쌍이 화면 안에 배치되고 가로 오버플로가 없음을 확인했다.
- `git diff --check`가 공백 오류 없이 통과했다.

### 남은 작업

- 링크 대상인 가로·세로 프레젠테이션 페이지 패키지를 구현한다.
- 임시 PDF 안내 callback은 전용 내보내기 서비스 구현 후 교체한다.
- 최종 정리 단계에서 루트 `seminar.html`, 기존 renderer와 세미나 CSS를 제거한다.

## 2026-08-03 — 프레젠테이션 페이지와 반응형 헤더 이전

### 요청과 목적

- 가로 발표와 세로 읽기 화면이 같은 페이지 초기화 계약과 공용 컴포넌트를 사용하게 한다.
- 연결 후 속성이 설정될 때 빈 내용이 남던 슬라이드 헤더를 반응형으로 수정한다.
- 데이터에 없는 topic 요청을 대체할 때 화면과 전환 URL이 실제 주제를 일관되게 가리키도록 한다.

### 파악한 문제

- 기존 `slide-header`는 `connectedCallback`에서 한 번만 렌더링해 페이지 모듈이 나중에 설정한 제목, 배지와 링크를 반영하지 못했다.
- 가로와 세로 HTML에 초기화 코드가 중복되고 세로 화면에서도 불필요한 controller가 생성됐다.
- 잘못된 topic을 파이썬 데이터로 대체해도 모드 전환 링크는 원래의 잘못된 요청 문자열을 유지했다.
- 렌더러, controller와 스타일 경로가 추상적인 `level4-presentation` 계층에 묶여 있었다.

### 결정과 근거

- 두 HTML은 `body[data-presentation-mode]`만 다르게 선언하고 `page.js`가 데이터 선택, 헤더 설정과 모드별 조립을 담당한다.
- `resolvePresentationState`를 순수 함수로 분리해 잘못된 주제 대체와 인쇄 파라미터를 자동 검증한다.
- 슬라이드 헤더는 모든 입력 속성을 관찰하고 연결 및 실제 속성 변경 시 같은 `render()`를 호출한다.
- 읽기 모드에는 counter와 이동 버튼을 만들지 않고 가로 모드에만 `PresentationController`를 연결한다.
- 공용 renderer는 세미나 URL을 입력받아 특정 페이지 경로를 내부에 하드코딩하지 않는다.

### 수행한 작업

- `pages/presentation/`에 가로·세로 HTML, 공용 `page.js`와 페이지 예외 CSS를 추가했다.
- 문서 renderer, 슬라이드 renderer와 이동 controller를 최상위 `components/`로 이전했다.
- `components/slide-header.js`를 관찰 속성 기반 Web Component로 다시 구성했다.
- 기존 header, layout과 card 스타일을 `styles/components/presentation.css`로 통합하고 인쇄 규칙을 공통 `styles/print.css`에 병합했다.
- 상호작용 요소에 포커스가 있을 때 Space·화살표 이동이 링크나 버튼 동작을 가로채지 않도록 controller 키보드 처리를 제한했다.

### 검증 결과

- 구현 전에 새 페이지 패키지, 컴포넌트와 공통 스타일이 없어 프레젠테이션 계약 테스트가 실패하는 것을 확인했다.
- `npm test`에서 모드·주제 대체, 실제 topic ID, 인쇄 상태와 읽기용 헤더 출력을 포함한 테스트 7개가 모두 통과했다.
- 실제 Chrome 브라우저에서 파이썬과 웹 주제의 가로·세로 네 조합이 제목, 배지와 실제 topic을 유지하는 전환 링크를 표시하는 것을 확인했다.
- 가로 화면에서 슬라이드 5개, 초기 `1 / 5`, 이전·다음 버튼과 클릭, ArrowRight 및 Space 이동을 확인했다.
- 세로 화면에서 읽기 섹션 4개가 렌더링되고 counter와 이동 버튼이 생성되지 않는 것을 확인했다.
- 존재하지 않는 topic 요청이 파이썬 주제와 `topic=python-intro` 전환 링크로 함께 대체되는 것을 확인했다.
- 390×844 모바일 뷰포트에서 고정 헤더와 슬라이드가 가로 오버플로 없이 표시되는 것을 확인했고 콘솔 오류·경고가 없었다.
- `git diff --check`가 공백 오류 없이 통과했다.

### 남은 작업

- 세미나 다운로드 callback을 실패 시에도 임시 DOM을 정리하는 PDF 서비스로 교체한다.
- 최종 정리 단계에서 기존 `slides/`, `level4-presentation` 구현과 사용하지 않는 `slide-controller.js`를 제거한다.

## 2026-08-03 — PDF 내보내기 서비스와 정리 보장

### 요청과 목적

- 세미나 목록 renderer에 섞여 있던 PDF용 DOM과 내보내기 로직을 비-UI 서비스로 분리한다.
- 실제 viewer와 동일한 가로 컨테이너 구조를 사용하고 성공, fallback과 예외 모든 경로에서 임시 DOM을 제거한다.
- PDF 엔진을 사용할 수 없는 환경에서도 현재 주제와 형식을 유지한 인쇄 화면을 제공한다.

### 파악한 문제

- 기존 가로 PDF 경로는 `.slide-container.horizontal` 요소를 직접 만들지 않아 카드 배치 보정 코드가 적용될 대상이 없었다.
- 임시 DOM 제거가 성공 경로의 `try` 끝에만 있어 exporter가 throw하면 요소가 문서에 남았다.
- PDF 상태 UI와 renderer, 데이터 선택, 내보내기 옵션 및 fallback URL이 한 컴포넌트에 결합돼 있었다.
- 로컬 Chrome 검증 환경에서는 외부 CDN의 `html2pdf` 전역이 로드되지 않아 직접 다운로드 대신 fallback이 필요한 상태였다.

### 결정과 근거

- `services/pdf-exporter.js`가 렌더 영역 생성, A4 옵션, 내보내기와 `finally` 정리를 단독 소유한다.
- renderer는 callback으로 주입하고 서비스는 presentation URL이나 페이지 모듈을 알지 못하게 했다.
- 가로 대상은 실제 `.slide-container.horizontal` 구조로 만들고 1024×724 비율과 카드별 page break를 적용했다.
- `html2pdf`가 없으면 페이지가 주제와 모드를 유지한 `print=true` URL을 새 탭으로 연다.
- 버튼의 disabled, loading과 아이콘 복구는 목록 컴포넌트가 callback 결과와 관계없이 보장한다.

### 수행한 작업

- `createPdfRenderZone`, `createPdfOptions`, `exportSeminarPdf`를 제공하는 PDF 서비스를 추가했다.
- 세미나 페이지에 기존 `html2pdf` CDN 엔진과 PDF 서비스를 연결하고 읽기·발표 renderer를 mode에 따라 주입했다.
- `createPrintFallbackUrl`로 현재 topic과 mode를 보존하는 인쇄 URL을 한 곳에서 생성했다.
- 세미나 목록 컴포넌트에 다운로드 중 버튼 잠금, 로딩 표시와 `finally` 복구를 추가했다.
- 클라이언트 PDF와 인쇄 fallback의 장기 결정을 `docs/decisions.md`에 기록했다.

### 검증 결과

- 구현 전에 PDF 서비스와 fallback URL export가 없어 서비스 계약 테스트가 실패하는 것을 확인했다.
- `npm test`에서 가로 렌더 구조와 카드 5개, page break, exporter 의도적 실패 후 정리, 엔진 부재 fallback 및 URL 보존을 포함한 테스트 9개가 모두 통과했다.
- 버튼 callback이 진행 중일 때 disabled와 loading 상태를 사용하고 완료 후 원래 아이콘과 상태로 복구되는 것을 자동 검증했다.
- 실제 Chrome 브라우저용 임시 하네스에서 가로 target 1개와 카드 5개를 확인했다.
- 브라우저 exporter를 의도적으로 throw한 후와 fallback 실행 후 `#pdf-temp-render-zone`이 각각 0개이고 콘솔 오류가 없음을 확인한 뒤 임시 하네스를 제거했다.
- 로컬 브라우저에서 CDN 엔진이 로드되지 않은 상태를 확인했으며 이 경우 인쇄 fallback이 호출되는 것을 확인했다.
- `git diff --check`가 공백 오류 없이 통과했다.

### 남은 작업

- 구 세미나·프레젠테이션 코드와 CSS, 중복 콘텐츠 및 과거 로그 디렉터리를 제거한다.
- 최종 Vercel 통합 검증에서 CDN 사용 가능 여부와 `/api/health`를 다시 확인한다.

## 2026-08-03 — 레거시 제거와 구조 개편 최종 검증

### 요청과 목적

- 새 페이지 패키지와 책임 기반 구조가 유일한 구현이 되도록 중복 진입점, 컴포넌트, CSS와 콘텐츠를 제거한다.
- 현재 문서는 실제 구조와 앞으로 지킬 확장 절차만 설명하고 과거 과정은 연도별 이력에 보존한다.
- 자동 테스트와 실제 브라우저에서 전체 사이트를 회귀 검증한다.

### 파악한 문제

- 새 구현이 동작해도 구 진입 HTML, 계층 기반 코드·CSS와 중복 홈 콘텐츠가 함께 남아 단일 출처가 아니었다.
- `docs/status.md`가 이미 해결된 초기 코드 리뷰 항목을 현재 문제로 계속 표시했다.
- README가 완료된 임시 구현 계획을 현재 문서로 연결하고 주요 URL과 주제 추가 절차를 제공하지 않았다.
- 프로젝트 로컬 Vercel CLI는 실행됐지만 현재 환경에 인증 정보가 없어 서버 시작 전에 로그인을 요구했다.

### 결정과 근거

- 구 구조는 호환 요구가 확인되지 않았으므로 리다이렉트나 중복 사본 없이 제거했다.
- 초기 코드 리뷰의 문제와 해결 과정은 이 연도별 이력에 모두 보존됐으므로 개별 `log/` 문서는 제거했다.
- 완료된 세부 구현 계획은 현재 구조 문서와 작업 이력이 대체하므로 저장소에서 제거했다.
- Vercel 로그인과 프로젝트 연결은 외부 계정 상태를 변경하므로 임의로 진행하지 않고 사용자 수행 단계로 남겼다.

### 수행한 작업

- 구 세미나 진입점, 주제별·viewer HTML, 중복 홈 조각과 사용되지 않는 renderer/controller를 제거했다.
- 계층 번호 기반 CSS와 중복 홈 컴포넌트 CSS, 오래된 구조 검증기 및 개별 코드 리뷰 로그를 제거했다.
- 구조 검증기에 금지 경로, 구 참조와 HTML·CSS 로컬 참조 유효성 검사를 추가했다.
- `docs/architecture.md`를 현재 디렉터리 구조와 주제·화면·API 확장 절차 중심으로 정규화했다.
- README에 실제 URL, 세미나 추가 절차, 문서 색인과 최초 Vercel 인증 방법을 추가했다.
- 현재 상태에서 해결된 문제를 제거하고 남은 외부 CDN, Vercel 인증과 의존성 감사 항목만 유지했다.

### 검증 결과

- 레거시 제거 전에 새 금지 경로 테스트를 실행해 기존 경로 16개와 깨진 내부 참조 4개 때문에 실패하는 것을 확인했다.
- 제거 후 `npm test`에서 개발 기반, 홈, 세미나, 프레젠테이션, PDF, 레거시 부재와 내부 참조를 검사하는 테스트 11개가 모두 통과했다.
- `git diff --check`가 공백 오류 없이 통과했다.
- Python 정적 서버와 실제 Chrome에서 홈, 세미나, 두 주제의 가로·세로 presentation 등 6개 URL을 데스크톱과 390×844 모바일 뷰포트로 확인했다.
- 모든 화면에서 기대한 제목과 콘텐츠 수, 주제 유지 모드 전환 링크, 가로 오버플로 부재와 콘솔 오류·경고 부재를 확인했다.
- 가로 presentation에서 다음 버튼, ArrowRight와 Space 이동이 `1 / 5`에서 차례로 counter를 갱신하는 것을 다시 확인했다.
- Vercel CLI 58.4.4가 Node.js v24.18.1에서 시작되는 것을 확인했지만 인증 정보가 없어 로그인 대기 상태에서 중단했다.
- `/api/health`의 함수 계약은 자동 테스트에서 HTTP 200 Response와 `{ "status": "ok" }` JSON을 확인했으며 Vercel HTTP 통합은 인증 후 수행 단계로 남겼다.

### 남은 작업

- 사용자가 로컬 Vercel 통합 확인을 원할 때 `npx vercel login`, `npx vercel link` 후 `npm run dev`를 실행한다.
- 사이트 공개를 시작할 때 Vercel Preview에서 6개 화면과 `/api/health`를 재검증한다.
- 상위 Vercel CLI 업데이트 시 기록된 전이 의존성 감사 항목과 직접 PDF CDN 로딩 상태를 다시 확인한다.

## 2026-08-03 — Vercel 개발 명령 재귀 오류 수정

### 요청과 목적

- 프로젝트 연결 후 `npm run dev`가 재귀 실행 오류로 종료되는 원인을 파악하고 로컬 정적 화면과 Functions 통합 실행을 복구한다.

### 파악한 문제

- 기존 `package.json#scripts.dev` 값이 `vercel dev --listen 3000`이었다.
- Vercel CLI 58.4.4는 실행 초기에 `scripts.dev`가 `vercel dev`를 포함하는지 검사하고, 포함하면 Development Command로 다시 호출될 가능성이 있다고 판단해 즉시 중단한다.
- 연결된 프로젝트는 Framework Preset이 `Other`이고 원격 Development Command override가 없었으므로 원격 설정이 아니라 로컬 npm 스크립트 이름이 직접 원인이었다.

### 결정과 근거

- Vercel CLI의 예약 검사 대상인 `dev` 스크립트를 제거하고 같은 명령을 `dev:vercel`로 제공한다.
- 별도 정적 서버나 프레임워크를 추가하지 않고 Vercel의 `Other` preset 기본 정적 파일 처리를 그대로 사용한다.
- 이 충돌이 돌아오지 않도록 `scripts.dev` 부재와 `scripts.dev:vercel` 값을 자동 테스트로 고정한다.

### 수행한 작업

- `package.json`의 로컬 통합 명령을 `npm run dev:vercel`로 변경했다.
- README, 작업 규칙, 구조·규칙·결정·상태 문서의 현재 명령을 함께 갱신했다.
- Vercel 로컬 실행 스크립트의 재귀 방지 회귀 테스트를 추가했다.

### 검증 결과

- 수정 전에 동일한 `npm run dev` 오류를 재현하고 새 회귀 테스트가 기존 `scripts.dev` 값 때문에 실패하는 것을 확인했다.
- `npx vercel project inspect portfolio`로 Framework Preset `Other`, Node.js 24.x와 별도 Development Command override 부재를 확인했다.
- 수정 후 자동 테스트 12개가 모두 통과했다.
- `npm run dev:vercel`이 Vercel CLI 58.4.4와 Node.js v24.18.1에서 재귀 오류 없이 `http://localhost:3000`을 시작했다.
- Vercel 로컬 서버를 통해 홈이 HTTP 200, `/api/health`가 HTTP 200과 `{ "status": "ok" }`를 반환하는 것을 확인했다.

### 남은 작업

- 실제 배포를 시작할 때 Vercel Preview에서 전체 화면과 `/api/health`를 다시 검증한다.

## 2026-08-03 — 모듈화 규칙과 세미나 작성 시스템 핸드오프

### 요청과 목적

- 프로젝트를 다시 검수해 긴 파일과 복잡한 책임을 줄일 모듈화 기준을 정한다.
- 모든 작업에서 계획을 먼저 정하고 발견한 문제를 해결 목록으로 관리하며, 사용자 제안에는 독립적인 의견을 함께 제시하는 규칙을 저장소에 남긴다.
- 세미나 작성 시스템은 새 채팅에서 구현할 수 있도록 지금까지 확정된 목표와 열린 결정을 구분해 전달한다.

### 최초 작업 목록

1. 현재 파일 크기, 책임과 중복 구현을 검수한다.
2. 모듈 크기와 선택적 문서화 기준을 현재 규칙에 반영한다.
3. 세미나 작성 시스템의 확정 사항과 열린 결정을 분리한다.
4. 새 채팅용 설계 핸드오프를 만들고 README와 현재 문서에서 연결한다.
5. 자동 테스트와 문서 검증을 수행한다.

### 문제와 해결 목록

- `해결`: 줄 수만으로 파일을 나누면 작은 파일이 과도하게 늘 수 있다. 한 책임, 독립 변경과 독립 검증 가능성을 우선 기준으로 정하고 200줄 권장·300줄 검토 기준을 보조 가드레일로 채택했다.
- `해결`: 모든 모듈에 별도 문서를 두면 코드와 문서가 쉽게 어긋날 수 있다. 복잡한 기능 디렉터리만 README를 두고 작은 계약은 JSDoc으로 설명하도록 범위를 제한했다.
- `확인`: 통합 테스트와 프레젠테이션 CSS가 300줄을 넘고 HTML 이스케이프가 여러 렌더러에 중복되어 있다. 이번 작업은 설계 문서화로 제한하고 실제 분리는 일반 모듈화 후속 작업으로 남겼다.
- `확인`: PDF 서비스에 임시 DOM 구성과 내보내기 옵션이 함께 있다. 렌더 영역과 내보내기 생명주기를 우선 분리 후보로 기록했다.
- `보류`: 세미나 슬라이드와 읽기 자료의 공통 원본 방식, 정확한 콘텐츠 예산과 이미지 메타데이터는 사용자 승인이 필요하다. 새 채팅의 열린 결정 목록으로 이동했다.
- `해결`: 비대화형 검증 셸의 `PATH`에 `npm`이 없어 첫 테스트 명령을 실행하지 못했다. 설치된 fnm의 Node.js 24 환경을 명시하는 `/Users/snow0821/.local/bin/fnm exec --using=24 npm test`로 다시 실행했다.
- `해결`: 자체 검토에서 아직 구현하지 않은 파일 길이 자동 검증이 현재 동작하는 것처럼 읽히는 문구와 기존 결정 상태의 잘못된 변경을 발견했다. 자동화는 후속 작업임을 명시하고 기존 상태를 복원했다.

### 결정과 근거

- 실행 JavaScript, CSS와 테스트는 200줄 이하를 권장하고 300줄을 넘으면 분리하거나 예외 근거를 남긴다. 자동 검증은 일반 모듈화 후속 작업에서 추가한다.
- 파일이 세 개 이상이거나 공개 계약, 생명주기 또는 의존 방향이 복잡한 기능만 디렉터리 README를 둔다.
- 작업 전 계획, 작업 중 문제 상태, 완료 후 해결과 검증을 하나의 작업 주기로 만들고 사용자 제안에는 기술적 의견과 trade-off를 포함한다.
- 세미나는 공통 규약, 맥락, 세부 콘텐츠, 구조화 이미지, 검증과 결과물 생성이 연결된 작성 시스템으로 다룬다.
- 세미나 코드는 이번 작업에서 변경하지 않고 권장안과 열린 결정을 핸드오프 문서에서 명확히 구분한다.

### 수행한 작업

- `AGENTS.md`와 `docs/conventions.md`에 작업 수행 순서, 문제 해결 목록, 의견 제시, 파일 크기와 선택적 모듈 문서 규칙을 추가했다.
- `docs/decisions.md`에 책임 기반 모듈화와 추적 가능한 작업 주기를 현재 결정으로 기록했다.
- `docs/status.md`에 검수에서 확인한 긴 파일, 중복 책임, 세미나 콘텐츠 계약 부재와 후속 순서를 기록했다.
- 새 채팅의 시작점인 `docs/superpowers/specs/2026-08-03-project-modularization-and-seminar-handoff-design.md`를 작성하고 README와 구조 문서에서 연결했다.
- 사용자 소유 변경인 `.gitignore`와 `.vscode/`는 수정하거나 이번 작업 범위에 포함하지 않았다.

### 검증 결과

- 설계 핸드오프에서 확정 사항, 기술적 권장안과 사용자 승인이 필요한 열린 결정이 구분되는지 자체 검토했다.
- 플레이스홀더, 상충하는 기술 제약, 누락된 후속 결정과 잘못된 로컬 문서 링크를 검사했다.
- 핸드오프가 참조하는 현재 문서와 구현 파일이 모두 존재하고 핸드오프에 `TBD`, `TODO`, 템플릿 경로와 행 끝 공백이 없음을 확인했다.
- `/Users/snow0821/.local/bin/fnm exec --using=24 npm test`에서 자동 테스트 12개가 모두 통과했다.
- `git diff --check`가 공백 오류 없이 통과했다.
- 사용자 화면이나 동작을 변경하지 않은 문서 작업이므로 브라우저 검증은 수행하지 않았다.

### 남은 작업

- 일반 모듈화 작업에서 통합 테스트, 프레젠테이션 CSS, 공통 HTML 안전 유틸리티와 PDF 서비스의 책임을 테스트 우선으로 분리한다.
- 새 채팅에서 핸드오프의 열린 결정을 하나씩 승인한 뒤 세미나 작성 시스템의 별도 사양과 구현 계획을 작성한다.
- 실제 배포를 시작할 때 Vercel Preview에서 전체 화면과 `/api/health`를 다시 검증한다.

## 2026-08-03 — 일반 모듈화 설계 승인

### 요청과 목적

- 두 후속 작업 중 일반 모듈화를 먼저 구현 가능한 설계로 확정한다.
- 특정 대화의 핸드오프 문서에 의존하지 않고 정규 문서와 모듈 README만으로 항상 인수인계 가능한 상태를 유지한다.

### 최초 작업 목록

1. 최소 변경형, 균형형과 세분화형 모듈화 접근을 비교한다.
2. presentation, PDF, CSS, 테스트와 공통 HTML 유틸리티의 목표 경계를 정한다.
3. 데이터 흐름, 오류 처리, 파일 길이 자동 검사와 회귀 검증 기준을 정한다.
4. 임시 설계와 핸드오프의 완료 후 제거 원칙을 정한다.
5. 승인된 설계를 사양으로 작성하고 자체 검토한 뒤 커밋한다.

### 문제와 해결 목록

- `해결`: 최소 변경형은 긴 파일만 줄이고 관련 presentation·PDF 책임이 계속 흩어지는 한계가 있다. 기능 디렉터리와 선택적 README를 사용하는 균형형을 선택했다.
- `해결`: 세분화형은 슬라이드 유형과 작은 함수까지 파일로 나눠 현재 규모보다 탐색 비용이 커진다. 공개 계약은 유지하고 독립 변경 가능한 경계만 분리한다.
- `해결`: 전용 핸드오프 문서는 시간이 지나면 정규 문서와 중복되거나 낡을 수 있다. 작업 중 임시 사양만 허용하고 완료 시 보편적인 내용은 정규 문서, 과정은 이력에 흡수한 뒤 삭제한다.
- `해결`: 사양의 일반 경로 표기 `docs/history/<year>.md`가 미완성 플레이스홀더로 해석될 수 있었다. 의미는 유지하면서 완성된 예시인 `docs/history/YYYY.md`로 바꿨다.
- `확인`: 일반 모듈화 구현 전까지 300줄 초과 통합 테스트와 프레젠테이션 CSS, 중복 HTML 유틸리티와 복합 PDF 서비스가 남아 있다. 승인된 사양의 구현 대상으로 유지한다.

### 결정과 근거

- presentation 컴포넌트, PDF 서비스와 프레젠테이션 스타일은 기능별 디렉터리로 묶고 테스트는 도메인별로 나눈다.
- 실행 파일은 200줄 이하를 기본으로 하며 201~300줄은 문서화된 예외, 300줄 초과는 실패하도록 자동 검증한다.
- URL과 사용자 기능은 변경하지 않고 import 경로, 내부 책임과 안전한 문자열 처리만 정리한다.
- 구현 완료 후 임시 설계·핸드오프를 제거하고 세미나 작성 시스템의 목표와 열린 결정만 현재 상태에 남긴다.

### 수행한 작업

- 사용자와 구조, 데이터 흐름, 오류 처리, 자동 검증과 문서 수명주기를 순서대로 검토하고 승인받았다.
- 승인된 내용을 `docs/superpowers/specs/2026-08-03-general-modularization-design.md`에 작성했다.
- 구조 문서의 임시 사양 표기를 개별 파일명이 아닌 작업 중 사양 영역으로 일반화했다.
- `docs/status.md`에 설계 사양의 사용자 검토 대기 상태를 반영했다.

### 검증 결과

- 설계 사양에서 플레이스홀더, 내부 모순, 범위 누락과 두 가지로 해석될 수 있는 요구사항을 자체 검토했다.
- 설계가 가리키는 현재 구현 파일과 기존 임시 핸드오프가 모두 존재하는지 확인했다.
- `/Users/snow0821/.local/bin/fnm exec --using=24 npm test`에서 자동 테스트 12개가 모두 통과했다.
- `git diff --check`가 공백 오류 없이 통과했다.
- 설계 문서만 변경했으므로 브라우저 검증은 수행하지 않았다.

### 남은 작업

- 사용자가 작성된 설계 사양을 검토하고 승인한다.
- 승인 후 테스트 우선 구현 계획을 작성한다.
- 계획에 따라 일반 모듈화를 구현하고 임시 사양을 정규 문서에 흡수한 뒤 제거한다.

## 2026-08-03 — 일반 모듈화 구현 계획 작성

### 요청과 목적

- 승인된 일반 모듈화 사양을 파일별 테스트 주기와 검증 명령이 있는 실행 계획으로 전환한다.
- 일반 모듈화 완료 후 구현 계획 자체도 임시 문서로 제거해 정규 문서만으로 인수인계 가능한 상태를 만든다.

### 최초 작업 목록

1. 테스트 분리와 공용 helper 구성을 첫 번째 독립 작업으로 만든다.
2. HTML 안전 유틸리티, presentation, PDF, CSS와 구조 가드를 각각 테스트 가능한 작업으로 나눈다.
3. 마지막 작업에 현재 문서 정규화, 임시 문서 제거와 브라우저 회귀를 포함한다.
4. 각 작업에 실패 확인, 최소 구현, 전체 테스트와 커밋 경계를 명시한다.

### 문제와 해결 목록

- `해결`: 파일 길이 가드를 먼저 적용하면 아직 분리하지 않은 478줄 CSS 때문에 중간 상태를 커밋할 수 없다. CSS 분리 후 실제 저장소 가드를 적용하되 정책 함수 자체는 실패 테스트부터 구현하도록 순서를 정했다.
- `해결`: 테스트 helper가 `node --test`의 직접 진입점으로 실행될 수 있다. package script를 `tests/*.test.mjs`로 제한하고 helper는 `tests/helpers/`에 둔다.
- `해결`: 완료된 설계 사양만 제거하고 구현 계획을 남기면 같은 인수인계 문제가 반복된다. 마지막 작업에서 설계 사양, 핸드오프와 구현 계획을 함께 정규 문서에 흡수한 뒤 삭제한다.
- `해결`: PDF 계획 초안이 fallback Promise를 반환한 직후 `finally` 정리를 실행할 수 있었다. 현재 계약과 같은 순서를 유지하도록 fallback을 `await`한 뒤 반환하도록 수정했다.
- `확인`: 실제 구현과 브라우저 검증은 아직 수행되지 않았다. 실행 방식 선택 후 계획의 일곱 작업으로 진행한다.

### 결정과 근거

- 테스트 기반, HTML 유틸리티, presentation, PDF, CSS, 구조 가드와 문서 정규화를 일곱 개의 독립 커밋 단위로 나눈다.
- 각 작업은 공개 인터페이스, 실패 테스트, 구현 코드, 검증 명령과 커밋 파일을 명시한다.
- 일반 모듈화가 끝나기 전에는 세미나 콘텐츠 계약과 작성 시스템을 변경하지 않는다.

### 수행한 작업

- `docs/superpowers/plans/2026-08-03-general-modularization.md`에 파일 맵과 일곱 작업의 테스트 우선 실행 절차를 작성했다.
- 설계 사양과 구조 문서에 완료된 구현 계획도 임시 문서 수명주기의 적용 대상임을 명시했다.
- `docs/status.md`를 구현 계획 작성 완료와 실행 방식 선택 대기 상태로 갱신했다.

### 검증 결과

- 구현 계획의 사양 커버리지, 플레이스홀더, 인터페이스 이름과 작업 간 경로 일관성을 자체 검토했다.
- 일곱 작업 모두 파일 목록, 공개 인터페이스, 실패 확인, 최소 구현, 검증과 커밋 단계를 가지는지 확인했다.
- 계획이 이동·삭제 대상으로 지정한 현재 파일이 모두 존재하고 작업 간 import 경로가 일치하는지 확인했다.
- `/Users/snow0821/.local/bin/fnm exec --using=24 npm test`에서 자동 테스트 12개가 모두 통과했다.
- `git diff --check`와 새 계획 문서의 행 끝 공백 검사가 통과했다.
- 구현 파일과 화면을 변경하지 않은 계획 작업이므로 브라우저 검증은 수행하지 않았다.

### 남은 작업

- 구현 실행 방식을 선택한다.
- 선택한 실행 절차로 일곱 작업을 수행하고 각 문제와 해결을 이력에 추가한다.

## 2026-08-03 — 일반 모듈화 구현

### 요청과 목적

- 승인된 균형형 구조와 테스트 우선 계획에 따라 일반 모듈화를 완료한 뒤 세미나 작성 시스템만 다음 작업으로 남긴다.
- 구현 중에도 계획, 발견 문제, 해결, 검증과 현재 상태를 지속해서 문서화한다.

### 최초 작업 목록

1. 통합 테스트를 도메인별 파일과 공용 helper로 분리한다.
2. 공통 HTML 이스케이프 유틸리티를 도입한다.
3. presentation 컴포넌트를 기능 디렉터리로 묶는다.
4. PDF 렌더 영역과 exporter 생명주기를 분리한다.
5. 프레젠테이션 CSS를 책임별로 나눈다.
6. 파일 길이와 로컬 import 자동 가드를 적용한다.
7. 정규 문서를 갱신하고 임시 문서를 제거한 뒤 전체 회귀를 검증한다.

### 문제와 해결 목록

- `해결`: 현재 `main`에 사용자 `.gitignore` 변경과 `.vscode/`가 있어 격리 worktree를 제안했으나 사용자가 덮어쓰기를 승인했다. `.gitignore`은 저장소 기준으로 복원하고 `.vscode/`와 이전 `.gitignore` 사본은 `/tmp/portfolio-user-changes.ZHEdyx`에 백업했다.
- `확인`: `npm install`에서 Vercel CLI 전이 의존성 감사 항목 30개와 install script 승인 경고가 다시 보고됐다. 기존 상태 문제이며 구조 변경 범위에서 강제 수정하지 않고 상위 CLI 업데이트 시 재검토한다.

### Task 1 — 테스트 기반 분리

- `RED`: 새 `foundation.test.mjs`가 `node --test tests/*.test.mjs`를 요구하도록 작성하고 실행해 기존 `tests/*.mjs` 값 때문에 1개 테스트가 실패하는 것을 확인했다.
- `GREEN`: 기존 통합 테스트 12개를 foundation, home, seminars, presentation, PDF와 structure 도메인으로 이동했다.
- `GREEN`: fake DOM과 파일 탐색을 `tests/helpers/`로 옮기고 package script가 root `*.test.mjs`만 실행하도록 변경했다.
- `문서`: 테스트 책임, helper 규칙과 실행 명령을 `tests/README.md`에 기록했다.
- `검증`: `/Users/snow0821/.local/bin/fnm exec --using=24 npm test`에서 12개 테스트가 모두 통과했고 helper가 직접 테스트로 실행되지 않았다.

### Task 2 — 공통 HTML 안전 유틸리티

- `RED`: 사이트 헤더에 따옴표, `&`와 태그 문자가 포함된 URL을 전달해 실제 `href`가 깨지는 테스트 실패를 확인했다.
- `RED`: 공통 `escapeHtml`과 `escapeAttribute` 계약 테스트가 `utils/html.js` 부재로 실패하는 것을 확인했다.
- `GREEN`: 텍스트와 속성 이스케이프를 분리한 `utils/html.js`를 추가하고 다섯 문자열 렌더러의 중복 함수를 제거했다.
- `해결`: 사이트 헤더가 링크 속성을 그대로 템플릿에 삽입하던 문제를 `escapeAttribute` 적용으로 해결했다.
- `해결`: 세미나 카드의 보이는 제목과 attribute용 제목을 구분해 따옴표가 있는 제목도 접근성 이름과 data attribute를 깨뜨리지 않게 했다.
- `검증`: focused 테스트 10개와 전체 자동 테스트 13개가 모두 통과했으며 컴포넌트에 로컬 escape 함수가 남지 않았음을 확인했다.

### Task 3 — presentation 기능 모듈

- `RED`: 새 `components/presentation/` 필수 파일 다섯 개의 부재와 구 최상위 컴포넌트 네 개의 존재로 presentation·structure 테스트 2개가 실패하는 것을 확인했다.
- `GREEN`: controller, 두 renderer와 reactive header를 `components/presentation/`으로 옮기고 페이지와 테스트 import를 새 경로로 갱신했다.
- `문서`: 공개 API, 생명주기, 의존 방향, 현재 스타일 위치, 오류와 확장 규칙을 `components/presentation/README.md`에 기록했다.
- `해결`: 내부 경로용 호환 re-export를 남기면 두 구조가 공존하므로 구 파일을 완전히 제거하고 저장소 내 모든 소비자를 한 번에 갱신했다.
- `검증`: presentation, seminars, PDF와 structure focused 테스트 6개가 모두 통과했다.

### Task 4 — PDF 서비스 생명주기 분리

- `RED`: `services/pdf/`의 README, exporter와 render-zone 부재 및 구 `services/pdf-exporter.js` 존재로 PDF·structure 테스트 2개가 실패하는 것을 확인했다.
- `GREEN`: 임시 DOM 구성과 layout 대기는 `render-zone.js`, PDF 옵션·엔진·fallback·정리 생명주기는 `exporter.js`로 분리했다.
- `문서`: 공개 API, 의존 방향, fallback과 모든 경로의 정리 보장을 `services/pdf/README.md`에 기록했다.
- `해결`: 동기 fallback만 사용한 기존 테스트는 `await` 제거 회귀를 잡지 못했다. 비동기 fallback 진행 중 render zone이 유지되는 테스트를 추가했다.
- `검증`: exporter에서 fallback `await`를 잠시 제거했을 때 새 테스트가 `0 !== 1`로 실패하고, 복원 후 PDF·seminars·structure focused 테스트 5개가 통과하는 mutation check를 완료했다.

### Task 5 — 프레젠테이션 CSS 분리

- `RED`: 네 presentation 스타일 파일 부재와 구 478줄 `presentation.css` 존재로 structure 테스트 2개가 실패하는 것을 확인했다.
- `GREEN`: header, layout, slide-card와 reading-document 스타일을 각 반응형 규칙과 함께 분리하고 `styles/main.css` import 순서를 고정했다.
- `문서`: `components/presentation/README.md`의 스타일 위치와 소유 규칙을 실제 디렉터리 구조로 갱신했다.
- `해결`: 첫 GREEN 실행에서 `presentation.test.mjs`의 필수 파일 목록이 구 CSS를 계속 요구했다. 네 새 파일을 요구하도록 테스트 참조를 정정했다.
- `검증`: 구 CSS와 네 새 파일의 선언 multiset을 비교해 속성·값 누락이 없음을 확인했다. 새 CSS는 각각 166, 66, 143, 110줄이며 전체 자동 테스트 14개가 통과했다.

### Task 6 — 모듈 경계 자동 검증

- `RED`: 파일 길이 정책과 로컬 ES module 참조 추출 함수를 먼저 테스트해 helper가 아직 없어 실패하는 것을 확인했다.
- `GREEN`: 실행 JavaScript, CSS와 테스트를 검사해 200줄 이하를 기본으로 유지하고 201~300줄은 사유가 기록된 예외만 허용하며 300줄 초과는 항상 실패하도록 구성했다.
- `GREEN`: 정적 import, re-export와 동적 import의 로컬 경로가 실제 파일 또는 페이지 디렉터리로 해석되는지 자동 검증한다.
- `문서`: 검사 범위, 예외 기준과 제외 대상을 `tests/README.md`에 기록했다. 현재 예외 목록은 비어 있다.
- `해결`: 정책 테스트를 기존 `structure.test.mjs`에 추가하면 그 테스트 파일 자체가 200줄에 가까워질 수 있어 책임이 분명한 `module-policy.test.mjs`로 분리했다.
- `해결`: 첫 GREEN 실행에서 테스트 fixture의 예제 import 문자열과 CSS 검사 문자열의 `@import`를 실제 JavaScript 참조로 오인했다. JavaScript 문맥의 import만 추출하고 fixture가 저장소 검사에 섞이지 않도록 구성했다.
- `검증`: 정책 테스트 파일 76줄, 구조 테스트 130줄, 파일 helper 50줄이며 전체 자동 테스트 16개와 `git diff --check`가 통과했다.

### Task 7 — 정규 문서화와 최종 회귀

- `RED`: 완료 후 `docs/superpowers/`가 없어야 하고 README가 임시 작업 문서를 가리키지 않아야 한다는 구조 테스트를 추가해 실제 임시 문서 때문에 실패하는 것을 확인했다.
- `GREEN`: 실제 presentation, PDF, 스타일, utility와 도메인별 테스트 구조를 `docs/architecture.md`에 반영하고 모듈 크기 자동 검증과 문서 수명주기를 규약·결정·작업 규칙에 통합했다.
- `문서`: 세미나 작성 시스템의 확정 목표와 여섯 열린 결정을 `docs/status.md`에 남겨 특정 대화 문서 없이 다음 설계를 시작할 수 있게 했다.
- `GREEN`: 완료된 설계 사양 두 개와 구현 계획 한 개, README의 임시 링크 및 비어 있는 `docs/superpowers/` 디렉터리를 제거했다.
- `해결`: 모바일 읽기 화면 첫 캡처에서 헤더 일부가 잘려 보이는 우려가 있었다. 390px와 320px에서 요소 경계, 텍스트 scroll width와 전체 페이지 overflow를 측정하고 재캡처한 결과 겹침이나 잘림이 재현되지 않아 불필요한 CSS 변경을 하지 않았다.
- `브라우저`: 데스크톱과 390×844 모바일에서 홈, 세미나 목록, Python·Web의 가로·세로 화면 여섯 URL을 확인했다. 두 목록 카드, 네 다운로드 접근성 이름, 주제별 슬라이드 5개, 읽기 섹션 4개, 주제·모드 링크, 슬라이드 이동과 가로 overflow 부재가 정상이고 console warning/error가 없었다.
- `검증`: 전체 자동 테스트 17개, `git diff --check`와 임시 디렉터리 부재 검사가 통과했다. 검사 대상 중 가장 긴 파일은 166줄이고 파일 길이 예외는 없다.

### 다음 작업

- 다음 작업에서는 `docs/status.md`의 열린 결정을 기준으로 세미나 작성 시스템의 별도 설계를 먼저 승인한다.
- 실제 배포를 시작할 때 Vercel Preview를 검증하고 공개 운영 시점에 `main` 보호 정책을 적용한다.

## 2026-08-03 — 세미나 도메인과 `60분 개론형` 설계

### 요청과 목적

- 1시간 동안 특정 주제를 전반적으로 소개하는 세미나를 독립된 유형으로 정의한다.
- 세미나 작성 규약은 방문자 화면이 아닌 내부 문서에 두고 프로젝트의 정규 문서 체계에 결정과 진행 상태를 남긴다.
- 콘텐츠뿐 아니라 레이아웃, 에셋, 렌더링과 출력까지 세미나 도메인이 함께 소유할 구조를 설계한다.
- 설계와 문서를 먼저 확정한 뒤 별도 계획으로 기존 구현 전체를 이전한다.

### 최초 작업 목록

1. 현재 세미나 페이지, 데이터, 컴포넌트, 서비스, 스타일, 테스트와 정규 문서를 확인한다.
2. 세미나 유형의 적용 범위, 대상 독자, 서사, 시간과 두 자료의 관계를 확정한다.
3. 문서 및 폴더 배치 대안을 비교하고 세미나 도메인의 소유 경계를 정한다.
4. 콘텐츠, 데이터 흐름, 레이아웃, 에셋, 오류와 검증 설계를 승인한다.
5. 승인된 설계 사양과 관련 현재 문서를 작성하고 자체 검토한다.
6. 자동 테스트와 문서 diff를 검증한 뒤 후속 구현 계획을 남긴다.

### 문제와 해결 목록

- `해결`: 모든 세미나에 60분을 강제하면 미래의 워크숍이나 심화 발표를 수용하기 어렵다. 여러 형식 중 첫 유형인 `introductory-60`으로 정의했다.
- `해결`: 대상 독자가 모호하면 개론의 설명 수준이 달라진다. 관련 분야에 관심은 있지만 전문 배경지식은 없는 일반인을 기본 독자로 확정했다.
- `해결`: 논문의 `선행연구`, `방법론` 명칭을 그대로 노출하면 일반인에게 불필요하게 학술적으로 보일 수 있다. 다섯 역할은 내부 의미 계약으로 유지하고 화면 제목은 쉬운 표현으로 바꿀 수 있게 했다.
- `해결`: 시간이나 글자 수 예산을 오류로 강제하면 발표와 함께 제공하는 읽기 자료에서 필요한 맥락이 빠질 수 있다. 정량적인 밀도는 경고로만 사용하고 실제 overflow, 잘림, 누락과 접근성 위반을 오류로 정의했다.
- `해결`: 발표 슬라이드와 읽기 자료를 별도로 작성하면 사실과 맥락이 어긋날 수 있고, 같은 본문을 그대로 쓰면 슬라이드 가독성이 떨어진다. 식별 가능한 공통 콘텐츠 블록을 두고 읽기 자료는 전체 맥락을 렌더링하며 발표 구성은 원본 블록을 참조하도록 설계했다.
- `해결`: `data/seminars/`만 도입하면 레이아웃, PDF와 스타일 규약이 계속 여러 최상위 폴더에 흩어진다. 세미나 전용 구현과 테스트 전체를 `features/seminars/`가 소유하고 `pages/`에는 URL 진입과 초기 조립만 남기기로 했다.
- `해결`: 페이지가 기능 패키지의 어느 수준까지 접근하는지가 모호하면 이전 후에도 깊은 import가 다시 생길 수 있다. 공개 진입점은 두 페이지 초기화 오케스트레이터만 제공하고 데이터 조회와 렌더러·서비스 연결은 기능 내부에 숨기도록 명시했다.
- `확인`: 내부 규약을 UI에서 노출하지 않기로 했지만 정적 저장소 파일에 대한 접근 차단을 보안 요구사항으로 정하지는 않았다. 현재 결정은 사이트에서 링크하지 않는다는 의미다.
- `확인`: 현재 Python·Web 데이터는 발표와 읽기 내용을 별도로 소유하므로 새 공통 원본으로 실제 변환하는 과정에서 콘텐츠 보강과 슬라이드 참조 설계가 필요하다.
- `보류`: 실제 파일 이동, 새 데이터 검증기와 런타임 오류 UI는 설계 검토 후 작성할 구현 계획까지 변경하지 않는다.

### 결정과 근거

- `features/seminars/` 아래에 형식, 데이터, 에셋, 컴포넌트, 레이아웃, 서비스, 스타일과 테스트를 둔다.
- 사이트 헤더, 공통 토큰·레이아웃과 범용 유틸리티처럼 실제 비세미나 소비자가 있는 항목만 전역에 유지한다.
- `introductory-60`은 `문제의 정의 → 기존 접근 → 핵심 방법론 → 활용 사례 → 결론과 한계`의 의미 구조를 사용한다.
- 핵심 방법론을 가장 깊게 설명하되 다른 단계도 방법론의 맥락을 만드는 필수 요소로 유지한다.
- `60분`은 대표적인 발표 상황이지 자료 분량 계약이 아니며 강사가 현장에서 시간과 강조를 조절한다.
- 발표와 읽기 자료는 하나의 구조화 원본을 사용하되 각 표현의 역할과 레이아웃은 분리한다.
- 규약은 실제 세미나를 만들며 확인한 근거로 점진적으로 개선한다.

### 수행한 작업

- `docs/seminar-domain-design.md`에 목표 구조, 소유 경계, 형식 규약, 공통 원본, 두 표현, 레이아웃, 에셋, 오류 처리, 이전 전략과 성공 기준을 작성했다.
- 현재와 승인된 목표 구조를 혼동하지 않도록 README와 아키텍처 문서에 구현 대기 상태 및 부분 이전 금지를 명시했다.
- 작성 규칙은 `docs/conventions.md`, 장기 결정은 `docs/decisions.md`, 진행 상태와 후속 작업은 `docs/status.md`에 반영했다.
- 실행 코드, CSS, 데이터와 페이지는 변경하지 않았다.

### 검증 결과

- 설계 사양에서 `TBD`, `TODO`, `FIXME`, 미정 요구사항과 불명확한 공개 경계를 검색했다. 플레이스홀더는 없었고 페이지와 기능 패키지 사이의 공개 API를 두 초기화 오케스트레이터로 명확히 했다.
- 목표 구조는 구현 대기, 현재 파일 경로는 유효 상태로 구분해 README, 아키텍처, 결정과 상태 문서 사이에 현재·목표 구조가 뒤섞이지 않는지 확인했다.
- `/Users/snow0821/.local/bin/fnm exec --using=24 npm test`에서 자동 테스트 17개가 모두 통과했다.
- `git diff --check`가 통과했다.
- 화면 동작과 레이아웃을 변경하지 않은 설계·문서 작업이므로 브라우저 검증은 수행하지 않는다.

### 남은 작업

- 사용자가 작성된 설계 사양을 검토한다.
- 승인 후 테스트 우선 구현 계획을 작성한다.
- 계획에 따라 세미나 구현 전체와 두 기존 주제를 이전하고 자동·브라우저 회귀를 수행한다.

### 설계 검토와 구현 계획

- 사용자가 작성된 설계 사양에 수정할 부분이 없음을 확인했다.
- 현재 VS Code 채팅과 다른 Codex 실행 환경을 비교한 결과, 설계 판단과 동일 로컬 작업 트리를 바로 사용할 수 있는 현재 채팅에서 계획을 이어가기로 권장했다. 핵심 컨텍스트는 커밋과 정규 문서에 남아 있어 다른 채팅에서도 복구할 수 있다.
- `docs/seminar-domain-plan.md`에 목표 파일 맵, 공개 인터페이스, 여덟 개의 독립 검토 단위, 각 RED/GREEN 명령, 커밋 경계, 브라우저 검증과 임시 문서 제거 절차를 작성했다.
- 구현 계획은 데이터 계약, 두 기존 주제 변환, 공통 렌더링, UI 컴포넌트, PDF·페이지 조립, 스타일·페이지 연결, 레거시 제거, 브라우저·정규 문서화 순서로 구성했다.
- `해결`: registry import 시 검증 예외를 발생시키면 사용자용 오류 상태를 렌더링할 수 없다. 모듈 import는 유지하고 목록·주제 조회 시 검증해 페이지 오케스트레이터가 예외를 표시하도록 계획을 정정했다.
- `해결`: `features/` 전체를 기존 파일 길이 검사에 추가하면 정책상 제외 대상인 주제 콘텐츠 데이터까지 실행 코드로 분류된다. 주제 데이터는 길이 기록에서만 제외하고 로컬 import 검사는 계속 받도록 계획했다.
- `해결`: 목록, 헤더, 제어기와 오류 상태 테스트를 한 파일에 합치면 200줄 기준을 넘길 위험이 있다. 목록과 presentation 컴포넌트 테스트를 두 파일로 나눴다.
- `해결`: 이미지 문자열 계약만 검사하면 실제 에셋 누락을 잡지 못한다. origin-rooted 주제별 에셋 경로를 강제하고 production 데이터 테스트에서 실제 파일 존재를 확인하도록 추가했다.
- 설계 사양의 열한 섹션을 계획의 전역 제약, 공유 인터페이스와 여덟 작업에 대조했다. 공개 함수명과 반환 형태, block·slide 필드, 테스트 경로와 후속 소비 관계를 일치시켰다.
- 플레이스홀더 금지 패턴을 검색했고 코드 fence 106개가 짝을 이루며 여덟 작업에 총 51개 checkbox 단계가 있는지 확인했다.
- `/Users/snow0821/.local/bin/fnm exec --using=24 npm test`에서 기존 자동 테스트 17개가 모두 통과했다.
- `git diff --check`와 새 계획 문서의 별도 공백 검사가 통과했다.
- 실제 구현은 아직 시작하지 않았으며 실행 방식 선택까지 보류한다.

### 구현 실행 준비

- 사용자가 작업별 구현자와 리뷰어를 분리하는 subagent-driven 실행 방식을 선택했다.
- 목표는 커밋 `60656fb` 이후 격리 worktree에서 계획의 여덟 작업을 순서대로 구현하고, 각 작업의 사양·품질 검토와 최종 전체 검토를 통과하는 것이다.
- 성공 기준은 기존 URL 유지, `introductory-60` 공통 원본 적용, 세미나 전용 구현의 기능 패키지 이전, 자동·브라우저 검증 통과와 정규 문서화 완료다.
- `확인`: 저장소가 일반 `main` checkout이고 `.worktrees/`가 존재하지 않으며 ignore 규칙도 없었다.
- `해결`: 현재 checkout과 사용자 `.vscode/` 변경을 격리하기 위해 `.worktrees/`를 `.gitignore`에 추가하고 전용 worktree를 만들기로 했다.

### 구현 재개 전 감사

- `확인`: 중단 이후 마이그레이션 파일, 별도 worktree와 완료 표시가 남아 있지 않았고 직전 Windows 구조 테스트 수정 세 파일만 작업 트리에 있었다.
- `해결`: Windows 구조 테스트 수정은 전체 자동 테스트 17개와 공백 검사를 다시 통과한 뒤 `5784e39`로 별도 커밋해 재개 기준점을 고정했다.
- `확인`: 도메인 경계, 공통 원본과 두 표현, 검증기부터 소비자 이전과 레거시 제거로 이어지는 전체 방향은 현재 코드와 일치해 유지한다.
- `해결`: Task 7의 저장소 상대 경로 비교가 슬래시를 전제로 해 Windows에서 오탐할 수 있으므로 OS 구분자를 정규화하도록 계획을 보정했다.
- `해결`: 이미지 블록 계약과 실제 overflow 검증 시점이 어긋나지 않도록 글꼴뿐 아니라 이미지 준비와 한 animation frame 이후 레이아웃을 검사하도록 계획을 보강했다.
- `해결`: macOS 사용자 절대 `fnm` 경로를 실행 전제로 삼지 않고 활성 Node.js 24와 플랫폼별 `npm` 명령을 사용하도록 계획에 실행 매핑을 추가했다.
- `해결`: 첫 `npm install`은 sandbox의 registry 접근 제한으로 실패했고 프로젝트 내부 캐시를 지정한 권한 승인 실행으로 Vercel 58.4.4 의존성을 설치했다. Windows 선택적 의존성 재계산으로 생긴 의도치 않은 lockfile diff는 기준 파일로 복원했다.
- `검증`: `codex/seminar-domain-migration` 브랜치의 격리 worktree에서 `npm.cmd test` 17개가 모두 통과했고 작업 트리가 깨끗함을 확인했다.

## 2026-08-03 — Seminar domain migration Task 3: shared semantic renderers

### Goal, tasks, and success criteria

- Goal: project the validated seminar source into reading and presentation markup without changing page entrypoints or public URLs.
- Tasks: add safe shared block rendering; add reading and slide projections; add layout overflow/readiness inspection; add focused contract tests.
- Success criteria: all five semantic sections appear in reading output, slides render only declared blocks without reading detail, all user-visible text and attributes are escaped, source data remains read-only, and inspection waits for fonts, images, and one animation frame.

### Findings and resolutions

- `confirmed`: the first focused test run failed with `ERR_MODULE_NOT_FOUND` for `features/seminars/layouts/reading-document.js`, proving the new test covered the missing Task 3 production modules.
- `resolved`: rendering is owned by `features/seminars/components/content-block.js` and the two feature-local layout modules. The seven supported block types share safe text/attribute escaping, while reading alone emits paragraph detail.
- `resolved`: presentation builds a private block-ID map, derives the agenda from source section titles, and makes every rendered slide a layout boundary. It does not modify source topic objects.
- `resolved`: layout inspection measures only descendants marked `data-layout-boundary`, uses a one-pixel tolerance, waits for font readiness, pending image load/error (or settled decode), then one animation frame.
- `confirmed`: no page or public-URL files changed in this task, so no browser run was applicable before Task 6 connects these renderers.

### Verification

- RED: `node --test features/seminars/tests/layouts.test.mjs` — 1 file failure, expected `ERR_MODULE_NOT_FOUND` for the absent reading-layout module.
- GREEN: `node --test features/seminars/tests/layouts.test.mjs` — 5 tests passed, 0 failed.
- Full: `npm.cmd test` — 33 tests passed, 0 failed.
- Hygiene: `git diff --check` completed with no output; all new executable/test files are within the 200-line limit (56, 25, 34, 37, and 91 lines respectively).

### Remaining work

- Continue with later migration tasks to connect the feature-local renderers to page orchestration and styles. No Task 3 blocker or deferred issue remains.

## 2026-08-03 — Seminar rendering contract review correction

### Goal, tasks, and success criteria

- Goal: resolve the approved shared-shape subtitle omission and close renderer-review gaps without changing the migration direction.
- Tasks: require and preserve subtitles, project complete core metadata, strengthen semantic rendering escaping, and complete image-readiness coverage.
- Success criteria: both sources expose their exact subtitles, validation rejects missing subtitles, both views render required metadata safely, and inspection settles image errors and waits for controlled decode before its animation frame.

### Findings and resolutions

- `confirmed`: the design and shared-shape already required `subtitle`, but Task 1 validation/fixture and Task 2 source data omitted it. The Task 3 cover consequently could not derive the required subtitle.
- `resolved`: `subtitle` is now required non-empty validation, the test fixture supplies it, and production data preserves `Introduction to Python` and `Introduction to Web Development`.
- `resolved`: presentation cover renders title, subtitle, summary, author, and updated metadata; the reading view also renders subtitle and tags with audience, prerequisites, and outcomes.
- `resolved`: review tests now isolate agenda markup from content titles, cover all seven block renderer fields plus projection attributes/links for escaping, verify pending image errors settle, and verify decode resolves before requesting the measurement frame.
- `resolved`: unsupported semantic blocks now throw the exact `지원하지 않는 세미나 블록: <type>` contract error instead of mojibake.

### Verification

- RED: `node --test features/seminars/tests/contract.test.mjs features/seminars/tests/data.test.mjs features/seminars/tests/layouts.test.mjs features/seminars/tests/rendering-escaping.test.mjs` — 22 tests; 17 passed and 5 failed as expected for missing subtitle validation/data/projection and the mojibake unsupported-block error.
- GREEN: the same focused command — 22 tests passed, 0 failed.
- Full: `npm.cmd test` — 39 tests passed, 0 failed.
- Hygiene: `git diff --check` completed with no output. Executable/test files range from 27 to 200 lines; the validator is exactly at the 200-line limit.

### Remaining work

- No review correction is deferred. Later migration tasks still own page wiring, styles, and browser verification.

## 2026-08-03 — Seminar domain migration Task 4: feature-owned interaction components

### Goal, tasks, and success criteria

- Goal: copy seminar list and presentation interaction components behind `features/seminars/` without changing any legacy consumer or public page behavior.
- Tasks: add feature-local list, header, controller, and public-safe error modules; first create two focused feature-local tests; preserve accessible download and navigation behavior.
- Success criteria: escaped list markup and unique PDF labels remain safe, downloads expose and clear their pending state, presentation navigation works without acting on focusable controls, non-presentation headers omit navigation, and error markup leaks no exception detail.

### Findings and resolutions

- `confirmed`: Task 4 tests were created before the feature-local component modules so the initial run can prove the intended missing-module RED state.
- `resolved`: the list, slide-header custom element, and horizontal presentation controller are copied behind the feature boundary with only the HTML utility import rebased to `../../../utils/html.js`; legacy modules and page consumers are unchanged.
- `resolved`: the feature error state contains only generic Korean recovery guidance in an alert region and never receives an exception object or message.
- `resolved`: focused test fakes now keep separate previous/next click listeners so both boundary controls are exercised; the copied download icon and pending indicator remain `📥` and `⏳`.
- `confirmed`: browser verification is not applicable in Task 4 because no page, CSS, URL, or runtime consumer changed; Task 7 will connect these modules and Task 8 owns the browser matrix.

### Verification

- RED: `node --test features/seminars/tests/seminar-list.test.mjs features/seminars/tests/presentation-components.test.mjs` — 5 tests failed as expected with `ERR_MODULE_NOT_FOUND` for the four absent feature-local component modules.
- GREEN: the same focused command — 5 tests passed, 0 failed.
- Full: `npm.cmd test` — 44 tests passed, 0 failed.
- Hygiene: `git diff --check` completed with no output. All Task 4 executable/test files are within the 200-line limit: seminar list 75, slide header 69, controller 69, error state 9, list test 55, and presentation-components test 97 lines. Legacy files remained unmodified, but the first self-review missed exact list/header wording and incomplete interaction coverage; the correction below supersedes its preservation claim.

### Remaining work

- No Task 4 blocker or deferred issue remains. Later migration tasks will wire these feature-local modules into the existing page entrypoints.

## 2026-08-03 — Seminar domain migration Task 4 review correction

### Goal, tasks, and success criteria

- Goal: correct Task 4 behavior-preservation gaps found in review while retaining the feature boundary and unchanged legacy consumers.
- Tasks: restore exact list/header strings; cover rejected downloads, controller boundaries and observer updates; cover custom-element registration/reactivity with isolated globals; correct the earlier preservation claim.
- Success criteria: feature output matches the legacy wording and navigation behavior exactly, all added regressions are demonstrated RED then GREEN, and no test leaks global browser shims.

### Findings and resolutions

- `confirmed`: the feature list used `읽기 문서` instead of the legacy `읽기용 문서` in both its visible vertical-mode label and its accessible PDF label, and the feature header used `‹ Seminars` instead of `← Seminars`.
- `confirmed`: the first component tests did not prove rejected-download cleanup, navigation no-ops at both bounds, observer updates, or custom-element registration/reactivity.
- `resolved`: the exact legacy strings are restored and asserted directly; failure cleanup uses `assert.rejects` while preserving the existing successful pending-state path.
- `resolved`: controller tests cover Space, keyboard/click navigation, focusable-control suppression, boundary no-ops, and observer-driven state. Header tests load cache-busted modules against isolated `HTMLElement`/`customElements` fakes and restore globals through test cleanup.

### Verification

- RED: `node --test features/seminars/tests/seminar-list.test.mjs features/seminars/tests/presentation-components.test.mjs features/seminars/tests/slide-header.test.mjs` — 7 tests passed and 2 failed as expected: the list lacked `읽기용 문서` and the header lacked `← Seminars`.
- GREEN: the same focused command — 9 tests passed, 0 failed.
- Full: `npm.cmd test` — 48 tests passed, 0 failed.
- Hygiene: `git diff --check` completed with no output. The affected production files remain 75 and 69 lines; component tests are 74, 127, and 62 lines, all within the 200-line limit. Self-review confirmed the only production edits restore the two legacy strings and every global browser shim is restored by test cleanup.

### Remaining work

- No Task 4 review-correction blocker or deferred issue remains.

## 2026-08-03 — Windows 구조 테스트 경로 정규화

### 요청과 성공 기준

- 프로젝트 점검에서 발견한 Windows 구조 테스트 오탐을 우선 수정한다.
- OS 경로 구분자와 관계없이 테스트 자체와 연도별 이력 제외가 적용되고, 실제 레거시 경로 검사는 유지되어야 한다.
- 전체 자동 테스트와 `git diff --check`가 통과해야 한다.

### 문제와 해결 목록

- `확인`: Windows에서 `collectFiles()`가 반환한 절대 경로를 문자열로 자르면 역슬래시 상대 경로가 되지만, 제외 목록은 슬래시 경로를 사용해 두 대상이 일치하지 않았다.
- `RED`: `node --test tests/structure.test.mjs`에서 구조 테스트 4개 중 1개가 테스트 자체와 이력 문서의 과거 레거시 명칭을 오탐해 실패했다.
- `해결`: `node:path`의 `relative`와 `sep`을 사용해 저장소 상대 경로를 구한 뒤 비교용 경로만 슬래시 형식으로 정규화했다. 파일 탐색과 레거시 패턴 검사는 변경하지 않았다.

### 검증 결과

- `node --test tests/structure.test.mjs`에서 focused 구조 테스트 4개가 모두 통과했다.
- `npm test`에서 전체 자동 테스트 17개가 모두 통과했다.
- `git diff --check`가 통과했다. Windows Git의 예정된 LF→CRLF 변환 경고만 있었고 공백 오류는 없었다.
- 화면 동작과 레이아웃을 변경하지 않았으므로 브라우저 검증은 수행하지 않는다.

## 2026-08-03 — Seminar domain migration Task 5: feature-owned page orchestration

### Goal, tasks, and success criteria

- Goal: move PDF and page orchestration behind the seminar feature boundary while leaving all legacy page consumers and public URLs unchanged.
- Tasks: copy the PDF render-zone/export lifecycle; add injectable list and presentation initializers; expose only the two initializers through the feature facade; add focused regression tests.
- Success criteria: every PDF exit path removes its temporary render zone, print fallback remains awaited, explicit topic errors render generic alerts, layout inspection reports status asynchronously, and current pages continue using legacy modules.

### Findings and resolutions

- `confirmed`: the first focused run failed with `ERR_MODULE_NOT_FOUND` for `features/seminars/index.js` and `features/seminars/services/pdf/render-zone.js`, proving the tests reached the new feature boundary before implementation.
- `superseded`: the first Task 5 implementation retained the PDF lifecycle but did not preserve every exact filename, mode-default, header-label, overflow-log, or alert-text contract. The preservation correction below records the complete comparison and resolution.
- `resolved`: both initializers accept injected accessors. List resolution and presentation topic/render failures log the original error, render the generic alert state, and return `{ ok: false, error }` synchronously.
- `resolved`: presentation defaults only an absent topic ID to `python-intro`; an explicit unknown ID remains an error. Horizontal rendering creates the controller, requested printing schedules one 350 ms callback, and layout inspection reports either `ok` or `error` without removing content.
- `confirmed`: the initial implementation added an extra promise hop, so rejected inspection status was unavailable after one microtask. Calling the inspector synchronously and supplying the rejection handler directly fixed the specified timing; all focused tests then passed.
- `confirmed`: Task 5 does not connect the feature facade to current pages or alter public URLs, so browser verification is not applicable until the later page-wiring task. No unresolved runtime behavior requires a `docs/status.md` change.

### Verification

- RED: `node --test features/seminars/tests/pdf.test.mjs features/seminars/tests/pages.test.mjs` — 2 file failures, expected `ERR_MODULE_NOT_FOUND` for the absent facade and PDF render-zone modules.
- GREEN: `node --test features/seminars/tests/pdf.test.mjs features/seminars/tests/pages.test.mjs` — 8 tests passed, 0 failed.
- Full: `npm.cmd test` — 56 tests passed, 0 failed.
- Hygiene: all Task 5 executable/test files are within the 200-line limit (51, 42, 59, 88, 2, 67, 120, and 56 lines respectively). `git diff --check` is recorded after the final documentation update.

### Remaining work

- Task 6 will make the existing page entrypoints call the public facade and add the feature stylesheet; Task 7 will remove the legacy modules after consumers move. No Task 5 blocker remains.

## 2026-08-03 — Seminar domain migration Task 5 preservation correction

### Goal, tasks, and success criteria

- Goal: resolve review and source-comparison deviations in Task 5 while retaining the feature boundary and unchanged public page consumers.
- Tasks: restore exact PDF names/options, presentation mode and header contracts, layout log wording, download alert wording, and add focused regressions for URL encoding and missing downloads.
- Success criteria: feature orchestration matches the legacy observable behavior, including exact user-facing strings and default branches, while explicit unknown topics still never fall back.

### Findings and resolutions

- `confirmed`: the original feature PDF filename regex discarded hyphens and added new suffix underscores; it now matches the legacy `/[^\\w\\s가-힣-]/g` rule and exact `발표슬라이드`/`읽기용문서` suffixes with original A4 margins and orientation.
- `confirmed`: the initial feature initializer selected vertical for omitted or invalid modes, whereas legacy behavior selects vertical only for exact `vertical`; the feature now defaults all other values to horizontal.
- `confirmed`: feature header alternate labels, the layout overflow prefix, and the PDF alert wording diverged from legacy strings. They now exactly use `읽기용 문서 ↗`, `발표용 슬라이드 ↗`, `세미나 레이아웃 overflow: ...`, and the original `자료를 연 뒤 브라우저의 인쇄 기능` alert.
- `resolved`: the unknown-topic test now records lookups and proves only `missing` is requested even when `python-intro` would be available. URL tests prove encoded presentation paths and exact print fallback URLs; the download test proves the requested missing ID, original-error logging, and alert behavior.
- `confirmed`: no current page entrypoint, public URL, or legacy module changed, so browser verification remains deferred to the page-wiring/browser-matrix tasks. No unresolved runtime issue requires `docs/status.md`.

### Verification

- RED: `node --test features/seminars/tests/pdf.test.mjs features/seminars/tests/pages.test.mjs features/seminars/tests/seminars-page.test.mjs` — 6 passed and 5 failed as expected for the legacy header labels, mode normalization, overflow message, PDF filename, and alert text.
- GREEN: the same focused command — 11 tests passed, 0 failed.
- Full: `npm.cmd test` — 59 tests passed, 0 failed.
- Hygiene: all touched executable/test files are within 200 lines (42, 88, 59, 73, 139, 43, and 56 lines). `git diff --check` is run after this documentation update.

### Remaining work

- No Task 5 preservation issue remains. Task 6 still owns public page wiring and Task 8 owns browser verification.

## 2026-08-03 Seminar domain migration Task 6: feature styles and thin pages

### Goal, task list, and success criteria

- Goal: make the seminar list and presentation pages consume only the seminar
  public facade and feature CSS entrypoint while preserving URLs, mount IDs,
  presentation-mode attributes, page CSS, and the list-page PDF CDN script.
- Tasks: add the feature style cascade; transfer seminar and presentation style
  ownership without changing declarations; add the reading-summary callout
  class; replace page modules with URL parsing and public initialization; and
  align test ownership with the now-live feature boundary.
- Success criteria: all three pages load global, feature, then page CSS; page
  modules do not deep-import feature internals; the feature suite and full
  suite pass; all changed executable, CSS, and test files are within 200
  lines; and a local worktree smoke check renders the feature-owned pages.

### Findings and resolutions

- `confirmed`: the new structure test was written first and RED as intended:
  `node --test features/seminars/tests/structure.test.mjs` reported 0 passed,
  3 failed for the missing feature stylesheet, legacy page imports, and
  legacy global CSS ownership.
- `confirmed`: the reading-summary regression was added before production
  code; `node --test features/seminars/tests/layouts.test.mjs` reported 6
  passed, 1 failed because the summary block had no `callout-box` class.
- `resolved`: `features/seminars/styles/index.css` imports all seven feature
  sheets in contract order. The seminar card, header, layout, slide, reading,
  and print declarations now have feature ownership; global print retains only
  body, site-header, and home-profile rules. The split grid is renamed to
  `.slide-blocks--split`, and reading summaries add the retained
  `.callout-box` class.
- `resolved`: the three public HTML pages load the feature stylesheet between
  global and page CSS. Their modules use only `features/seminars/index.js`;
  the presentation module only parses URL state and calls the public
  initializer. URLs, DOM IDs, mode data attributes, page CSS, and the list PDF
  CDN script remain present.
- `confirmed`: the first complete suite after the page cutover reported 59
  passed and 3 failed. The obsolete root seminar page, presentation, PDF, and
  global presentation-cascade tests still required legacy page exports and
  global CSS imports despite passing feature equivalents.
- `resolved`: rather than adding transitional compatibility exports or
  accepting a red full suite, ownership cleanup was advanced into Task 6:
  obsolete root seminar tests were removed, the root cascade assertion was
  removed, and `tests/README.md` now separates root global policy from the
  feature seminar contract. `docs/seminar-domain-plan.md` records the
  sequencing correction and removes those three deletions from Task 7. The
  trade-off is earlier removal of redundant tests in exchange for retaining a
  single authoritative feature test boundary and a green suite.
- `confirmed`: an initial browser attempt on port 4173 served another checkout.
  A dedicated port 4174 was used instead; no source change was required.
- `resolved`: scoped review found that the feature renderer did not emit the
  preserved reading and slide layout classes, so the copied declarations would
  not apply after page cutover. Review also found that global print retained
  only the body background and lost the original width, margin, padding, and
  background-color declarations. Regression assertions first reported 7
  passed and 3 failed; the renderer now emits the compatible reading and slide
  classes, and global print restores those exact body declarations.

### Verification

- GREEN: `node --test features/seminars/tests/structure.test.mjs
  features/seminars/tests/layouts.test.mjs` passed 10 of 10 tests.
- Feature: `node --test features/seminars/tests/*.test.mjs` passed 45 of 45
  tests.
- Full: `npm.cmd test` passed 57 of 57 tests after the ownership correction.
- Smoke: `http://localhost:4174/pages/seminars/` rendered two cards;
  horizontal Python presentation rendered nine slides with
  `data-layout-status="ok"`; vertical Python presentation rendered the
  feature reading document with `data-layout-status="ok"`; all three pages
  exposed the feature stylesheet link and no console warning or error was
  recorded. The full desktop/mobile browser matrix and PDF fallback matrix
  remain Task 8 work.
- Hygiene: `git diff --check` produced no whitespace errors. Feature CSS line
  counts are 7, 38, 53, 66, 110, 143, 154, and 166; no scanned feature
  executable, CSS, or test file exceeds 200 lines. Consumer scans found no
  page deep import or legacy seminar import, and no feature runtime emission
  of the removed `.slide-split-grid` class.
- Review correction: the same structure and layout command passed 10 of 10;
  after reloading the dedicated worktree server, horizontal output contained
  nine `.slide-content-inner` cards and vertical output contained
  `.reading-doc-container` plus six `.reading-doc-section` elements. Both
  retained `data-layout-status="ok"` with no console warning or error.
- Independent review correction: a real Python rendering probe found that code
  blocks and reading metadata did not emit the moved style hooks. The new
  focused structure/layout/escaping command was RED at 7 passed and 4 failed:
  presentation and reading code roots lacked required classes, metadata lacked
  its wrapper, and the dead footer selector remained. `renderContentBlock` now
  safely appends injected classes to every block root; presentation gives code
  `slide-code-block`; reading gives code `slide-code-block reading-doc-code`
  and summaries `callout-box`; and the header groups escaped tags, author, and
  update metadata in `reading-doc-meta`. The unused `.slide-card-footer` rule
  and its mobile rule were removed after `rg` found no runtime emitter.
- Independent review GREEN: focused layout, structure, and escaping tests
  passed 14 of 14; feature tests passed 46 of 46; `npm.cmd test` passed 58 of
  58; `git diff --check` reported no whitespace error; emitter and line-count
  scans found no production footer selector and no file over 200 lines. On the
  dedicated worktree server, real horizontal and reading code blocks both had
  computed `rgb(29, 29, 27)` backgrounds and `overflow-x: auto`; the reading
  view exposed its metadata wrapper, and the console had no warning or error.

### Remaining work

- Task 7 still removes the now-unreferenced legacy production modules and
  styles and extends the final repository boundary scan. Task 8 owns the full
  browser and PDF/print fallback matrix. No Task 6 issue requires
  `docs/status.md`.

## 2026-08-03 Seminar domain migration Task 7: legacy ownership removal

### Goal, task list, and success criteria

- Goal: remove the now-unreferenced top-level seminar implementation and
  enforce the feature-only boundary without changing public URLs or facade
  exports.
- Tasks: add legacy-path, ownership, page-import, and feature module-policy
  assertions; remove the exact legacy files; update test ownership and feature
  boundary documentation; verify references, tests, and repository hygiene.
- Success criteria: the 17 listed legacy files and their empty owner
  directories are absent; only the facade is imported by page modules; feature
  modules are scanned with topic content excluded only from line limits; all
  focused and full tests pass.

### Findings, resolutions, and evidence

- `resolved`: RED first added the feature `forbiddenPaths` contract for all 17
  files plus owner-directory assertions and page facade checks. `node --test
  tests/structure.test.mjs tests/module-policy.test.mjs
  features/seminars/tests/structure.test.mjs` passed 8 and failed 1 exactly as
  intended, listing every legacy path still present.
- `resolved`: Removed exactly `components/seminar-list.js`, the five tracked
  `components/presentation/` files, `data/seminars.js`, the two tracked
  `data/topics/` files, the three tracked `services/pdf/` files,
  `styles/components/seminar-card.css`, and the four tracked
  `styles/components/presentation/` files. Only their resulting empty legacy
  directories were removed non-recursively; shared components, styles, utils,
  test helpers, and home files were retained.
- `resolved`: The first post-deletion focused run exposed an obsolete
  `services/` scan root, producing `ENOENT`. Removed that retired root while
  retaining the new `features/` scan. Repository-relative records and missing
  import reports now normalize Windows and POSIX separators; topic files still
  participate in import resolution and only skip line records.
- `confirmed`: The stale-reference command for old seminar components, data,
  PDF services, and styles returned no matches (rg exit 1 with empty output,
  the expected no-match result).
- `GREEN`: The focused structure and module-policy command passed 9 of 9.
  `npm.cmd test` passed 59 of 59. Browser work was not repeated here because
  this task only deletes unreferenced files; the full URL, layout, and PDF
  matrix remains Task 8 work.
- `resolved`: Review found the ownership assertion checked only `services/pdf`
  and the page-import matcher recognized only static `from` syntax. The
  feature test now asserts the top-level `services` owner is absent and reuses
  `extractLocalModuleReferences` for all feature references. A synthetic
  regression proves bare side-effect and dynamic deep imports are both found
  and rejected when they differ from the public facade.
- `RED`: The strengthened focused command passed 8 and failed 2: the old
  matcher returned no synthetic bare/dynamic references, and their literal
  fixture paths also surfaced as false missing imports in the repository scan.
  Runtime-built fixture paths keep the synthetic parser test real without
  injecting false repository imports.
- `GREEN`: The strengthened focused command passed 10 of 10. The review also
  removed Task 7's obsolete special staging requirement and related history
  claims for `.vscode/`; neither `.vscode/` nor `.gitignore` was changed.
- `GREEN`: Final `npm.cmd test` passed 60 of 60; the legacy stale-reference
  command still returned no matches, `git diff --check` had no whitespace
  errors, and the changed structure/module-policy tests were 146, 105, and 88
  lines respectively.
- `resolved`: A second scope review removed the remaining active `.vscode/`
  requirements from the plan's global constraints and Task 8 expected result.
  Both now use the generic requirement that only intended task changes remain;
  factual historical notes were not rewritten.

### Remaining work

- Task 8 owns the complete desktop/mobile browser and PDF/print-fallback
  matrix plus permanent-document normalization. No Task 7 blocker requires a
  `docs/status.md` entry.

## 2026-08-03 Seminar domain migration Task 7.5: PDF fallback gesture

### Goal, task list, and success criteria

- Goal: preserve the original PDF-button user activation when the external
  `html2pdf` CDN is unavailable so Chrome can open the matching print view.
- Tasks: test the failing fallback before production changes; correct the first
  script-open attempt after real-browser review; retain validation, direct
  export, and the generic exporter fallback; run focused and full tests.
- Success criteria: each PDF control is a native `print=true` link with
  `_blank` and `noopener noreferrer`; the no-CDN click keeps default navigation
  and creates no render zone; existing direct-export and error paths stay green.

### Findings, resolutions, and evidence

- `confirmed`: Local Chrome at
  `http://127.0.0.1:4173/pages/seminars/` exposed
  `typeof window.html2pdf === "undefined"`. The Python vertical PDF control
  restored its loading UI, but neither controlled tabs nor the browser's open
  tabs contained the expected `print=true` page.
- `confirmed`: The seminar-page download handler awaited `exportSeminarPdf`;
  that exporter created a render zone and awaited layout before invoking its
  fallback. Consequently `window.open()` ran after the originating click's
  user-activation window and Chrome blocked it.
- `RED`: `node --test features/seminars/tests/seminars-page.test.mjs` passed 2
  and failed 1. The new regression observed `[]` before the first yield instead
  of the exact vertical fallback call.
- `attempted`: The first correction called `window.open()` synchronously after
  valid-topic validation and returned before render-zone construction. The
  direct-export branch and generic exporter fallback remained unchanged.
- `false-green`: `node --test features/seminars/tests/seminars-page.test.mjs
  features/seminars/tests/pdf.test.mjs` passed 4 of 4. `npm.cmd test` passed 61
  of 61, but the new test proved only that a window mock received `open()`; it
  did not prove that Chrome created a browsing context.
- `failed browser recheck`: Both Playwright and browser user-action clicks on
  the Python vertical PDF control still created no fallback tab after commit
  `0a42995`. In the same browser, a native seminar link with `target="_blank"`
  did open a new tab, isolating the failure to the script-popup approach.
- `RED`: The second focused command passed 4 and failed 3. It showed that PDF
  controls were still buttons without fallback URLs, default navigation was
  always prevented, and the initialized page emitted no native print link.
- `resolved`: Both PDF controls are now native anchors with exact `print=true`
  URLs, `target="_blank"`, `rel="noopener noreferrer"`, `role="button"`, and
  four distinct accessible names across the two seminar cards. A synchronous
  predicate permits native navigation without loading or callback work when
  the CDN is absent; only an available `html2pdf` dependency selects the
  prevented, asynchronous direct-export path. Missing-topic validation remains
  in that direct path, and `exportSeminarPdf` retains its fallback contract.
  The feature README now records this native-fallback ownership contract.
- `GREEN`: `node --test features/seminars/tests/seminar-list.test.mjs
  features/seminars/tests/seminars-page.test.mjs
  features/seminars/tests/pdf.test.mjs` passed 8 of 8. `npm.cmd test` passed 62
  of 62, including direct-export, missing-topic, cleanup, and generic fallback
  coverage.
- `confirmed`: Review of the native-anchor correction found that assigning its
  nonexistent `.disabled` behavior did not prevent another direct-export click.
  The `role="button"` anchors also lacked Space-key activation.
- `RED`: `node --test features/seminars/tests/seminar-list.test.mjs` passed 3
  and failed 2. A repeated pending click invoked direct export twice instead of
  once, and Space did not prevent scrolling or synthesize activation.
- `resolved`: Each PDF control now owns a synchronous in-flight guard. Direct
  export sets `aria-disabled="true"`, prevents repeated direct-export clicks,
  and clears the ARIA state in `finally`. Space prevents its default scrolling
  behavior and invokes the control; Enter and no-CDN clicks remain native.
- `GREEN`: The focused seminar-list command passed 5 of 5. The feature README
  records the in-flight and keyboard contract.
- `resolved`: The first broader verification passed 8 and failed 1 because the
  page test's button double lacked the real anchor's attribute methods. The
  test double now mirrors `setAttribute` and `removeAttribute` and verifies
  that the missing-topic direct path also clears `aria-disabled`.
- `GREEN`: The broader list, page, and PDF command passed 9 of 9;
  `npm.cmd test` passed 63 of 63. The changed component and tests are 85, 170,
  and 80 lines, all within the 200-line limit.
- `failed browser recheck`: After commit `5be1e23`, focusing a PDF anchor and
  pressing Space through Chrome still left the seminar-list URL unchanged and
  created no tab before the 350 ms print trigger. The unit test had proved a
  synthetic click event, but that click still targeted `_blank` and remained
  subject to popup blocking.
- `RED`: `node --test features/seminars/tests/seminar-list.test.mjs` passed 5
  and failed 1: the Space-generated fallback click observed `_blank` instead of
  the required `_self`. The direct-export Space preservation test stayed green.
- `attempted`: A no-CDN Space activation temporarily targeted `_self` for the
  synchronous native click and restored `_blank` in `finally`. CDN-backed Space
  activation remains on the intercepted direct-export path; mouse and Enter
  retain native new-tab fallback behavior, names, guards, and ARIA state.
- `GREEN`: The focused seminar-list command passed 6 of 6. The component and
  test are 96 and 192 lines, both within the 200-line limit. The feature README
  distinguishes the Space same-tab fallback from mouse and Enter behavior.
- `GREEN`: The final list, page, and PDF command passed 10 of 10;
  `npm.cmd test` passed 64 of 64, and `git diff --check` reported no whitespace
  error. Real-Chrome confirmation remains the next Task 8 verification step.
- `failed browser recheck`: After commit `d2b3484`, both Playwright Space and a
  focused browser user-action Space left the seminar-list URL unchanged. The
  expected navigation to the exact horizontal `print=true` URL timed out, so
  temporary `_self` still did not make synthetic anchor activation reliable.
- `RED`: The list and page command passed 7 and failed 2. The component still
  emitted a synthetic click, while the injected safe location double received
  `[]` instead of
  `../presentation/horizontal.html?topic=sample&print=true`.
- `resolved`: The component now receives an explicit `navigateFallback(href)`
  boundary and calls it directly for no-CDN Space without a synthetic click.
  The page binds that boundary to `windowRef.location.assign`, while CDN-backed
  Space still emits the intercepted direct-export click. Mouse and Enter keep
  native `_blank` behavior, and names, guards, and ARIA state are unchanged.
- `GREEN`: The focused list and page command passed 9 of 9. The changed
  component, page, and tests are 92, 60, 194, and 100 lines, all within the
  200-line limit. The feature README records the explicit location boundary.
- `GREEN`: The final list, page, and PDF command passed 10 of 10;
  `npm.cmd test` passed 64 of 64, and `git diff --check` reported no whitespace
  error. Real Chrome will recheck the injected same-tab navigation in Task 8.

### Remaining work

- Task 8 will repeat the Chrome PDF/print fallback matrix against the native
  anchors and record the resulting print-view URL, orientation, content, and
  console state. Until that real-browser recheck passes, the second correction
  is verified at the DOM/event-contract and automated-suite levels only.
